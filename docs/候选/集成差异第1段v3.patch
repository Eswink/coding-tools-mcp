diff --git a/.github/workflows/异步命令验证v1.yml b/.github/workflows/异步命令验证v1.yml
index 175e52c..1920b4c 100644
--- a/.github/workflows/异步命令验证v1.yml
+++ b/.github/workflows/异步命令验证v1.yml
@@ -9,6 +9,25 @@ concurrency:
   group: async-command-v1-${{ github.ref }}
   cancel-in-progress: true
 jobs:
+  frontend:
+    runs-on: ubuntu-latest
+    timeout-minutes: 15
+    steps:
+      - uses: actions/checkout@v4
+        with:
+          persist-credentials: false
+      - uses: actions/setup-node@v4
+        with:
+          node-version: '22'
+      - run: npm ci
+      - run: npm run check
+      - run: npm run build
+      - name: 执行任务日志生产函数回归
+        shell: bash
+        run: |
+          set -euo pipefail
+          npx --no-install tsc src/lib/任务日志v2.ts --target ES2022 --module commonjs --strict --outDir "$RUNNER_TEMP/任务日志v2"
+          TASK_LOG_MODULE="$RUNNER_TEMP/任务日志v2/任务日志v2.js" node --test tests/任务日志回归v2.test.mjs
   regression:
     strategy:
       fail-fast: false
 diff --git a/docs/specs/异步命令v1/任务v1.md b/docs/specs/异步命令v1/任务v1.md
index 3354845..a7d4fa9 100644
--- a/docs/specs/异步命令v1/任务v1.md
+++ b/docs/specs/异步命令v1/任务v1.md
@@ -52,3 +52,23 @@
 | FR-8 | 技术方案 | 1.1,2.1,2.3,3.1 | 未开始 |
 
 范围复核：保留26个既有核心工具，增加4个；既有registry和call_tool_contract数量断言同步为30，新增四个名称逐项检查，不删除旧断言覆盖。
+
+
+## 第二阶段交付任务
+- [ ] 4.1 加密任务记录、每命名空间单密钥、独占锁、故障拒写及interrupted恢复。_需求: FR-2 FR-6 FR-7 FR-9_；设计: 持久任务设计v2。
+  - **证据块**: tools/异步命令v1.rs 的 reserve/run/cancel 与 context.rs 的 enable_durable_tasks 接线。
+- [ ] 4.2 Unix进程组和Windows暂停/加入Job/恢复，取消和超时清理。_需求: FR-5 FR-10_；设计: 持久任务设计v2。
+  - **证据块**: tools/异步命令v1.rs 的 reserve/run/cancel 与 context.rs 的 enable_durable_tasks 接线。
+- [ ] 4.3 24小时异步预算与普通exec十分钟策略隔离。_需求: FR-3 FR-8 FR-11_；设计: 持久任务设计v2。
+  - **证据块**: tools/异步命令v1.rs 的 reserve/run/cancel 与 context.rs 的 enable_durable_tasks 接线。
+- [ ] 4.4 任务面板、IPC只读查询和取消、增量UTF8与迟到响应防护。_需求: FR-4 FR-12_；设计: 持久任务设计v2。
+  - **证据块**: tools/异步命令v1.rs 的 reserve/run/cancel 与 context.rs 的 enable_durable_tasks 接线。
+- [ ] 4.5 完整双平台Rust/前端回归，review后修复，逐轮记录。_需求: FR-9 FR-10 FR-11 FR-12_；设计: 测试策略。
+  - **证据块**: .github/workflows/异步命令验证v1.yml 执行异步专项、完整回归与 -D warnings；tests/任务日志回归v2.test.mjs 运行真实生产函数。
+
+| 需求ID | 设计章节 | 任务编号 | 状态 |
+|---|---|---|---|
+| FR-9 | 持久任务设计v2/安全恢复 | 4.1,4.5 | 实施中 |
+| FR-10 | 持久任务设计v2/进程所有权 | 4.2,4.5 | 实施中 |
+| FR-11 | 持久任务设计v2/执行预算 | 4.3,4.5 | 实施中 |
+| FR-12 | 持久任务设计v2/桌面查询 | 4.4,4.5 | 实施中 |
diff --git a/docs/specs/异步命令v1/持久任务设计v2.md b/docs/specs/异步命令v1/持久任务设计v2.md
new file mode 100644
index 0000000..d44f25b
--- /dev/null
+++ b/docs/specs/异步命令v1/持久任务设计v2.md
@@ -0,0 +1,18 @@
+# 异步命令第二阶段：持久记录、受管进程与桌面查询
+
+## 接受范围
+
+提交、查询、取消仍为普通 MCP/Actions 工具，不声明原生 Tasks 扩展。执行与 HTTP 生命周期分离。当前任务列表及终态日志支持同一系统用户、工作区配置和服务通道恢复；MCP 与 Actions 不共享权限空间。
+
+1. 提交必须先保存加密的 request_id、指纹、job_id 和 queued 记录再启动命令；同键不同参数拒绝，不静默重复执行。
+2. 使用已有 AES-GCM/系统凭据库，记录放于应用 Harness 目录，不放项目目录。不新增明文命令或日志文件。损坏、缺密钥、未来版本拒绝加载并保留原文件。
+3. 一个服务命名空间由跨进程文件锁独占；进程内相同服务重建复用受管任务。应用退出后不承诺命令继续；重新加载非终态记录为 interrupted，保留未知状态与幂等键，绝不按陈旧 PID 接管/杀进程或自动重跑。
+4. 取消/超时作用于受管进程组或 Windows Job Object；Windows 在恢复主线程前加入 Job；附加失败拒绝运行。自行逃逸组/提权等不属于安全沙箱保证。
+5. 普通 exec_command 仍上限十分钟；异步默认十分钟，可由工作区配置允许最长24小时。请求 timeout_ms 是执行预算，不是 HTTP 等待。预算不能通过伪造参数扩大普通工具权限。
+6. 输出每流1MiB环形缓冲、绝对字节游标和丢弃计数维持；约2秒和终态加密快照。崩溃可能丢失最后未落盘输出，明确标记 interrupted/output_complete=false。
+7. 每通道4活动/32保留、终态1小时，未知终止不自动过期。仅明确人工核查确认终止后可释放 interrupted 的占用；仍保留去重键到保留期结束。
+8. 桌面工作区提供任务列表/状态/增量日志/取消；轮询不重叠、切换工作区不串结果、不自动唤醒GPT。
+
+## 验证与回退
+
+保留原200ms真实执行截止和非阻塞提交/取消断言；功能测试给予可解释的Windows启动预算。增加加密落盘/重启终态/未知恢复/损坏保护/通道隔离、长预算隔离、子孙进程清理、传输断连与前端检查。两平台all-targets、完整test、生产库-D warnings不可降级。v0.2.3与main不自动改动；本阶段在PR#2完整提交，通过后再决定合并与发布。
diff --git a/docs/specs/异步命令v1/设计v1.md b/docs/specs/异步命令v1/设计v1.md
index c79b631..0cec29f 100644
--- a/docs/specs/异步命令v1/设计v1.md
+++ b/docs/specs/异步命令v1/设计v1.md
@@ -1,3 +1,6 @@
+# 第二阶段修订说明
+本文件原v1边界为历史记录；当前执行边界以本目录持久任务设计v2.md和修订需求FR-9至FR-12为准，包含持久记录/中断恢复、进程组/Job Object、长预算和桌面控制。
+
 # 设计文档：MCP异步命令v1
 
 ## 概述
diff --git a/docs/specs/异步命令v1/需求v1.md b/docs/specs/异步命令v1/需求v1.md
index b6b0bb1..6035974 100644
--- a/docs/specs/异步命令v1/需求v1.md
+++ b/docs/specs/异步命令v1/需求v1.md
@@ -7,8 +7,8 @@
 job是进程执行任务；Harness start_task是开发计划，不是后台进程。timeout_ms为进程预算，不是HTTP等待预算。
 
 ## 范围边界
-In Scope：四个普通工具start_exec_task/get_exec_task/list_exec_tasks/cancel_exec_task，内存任务管理，现有命令引擎复用。
-Out of Scope：跨应用或服务重启持久恢复、交互式stdin/TTY、自动重跑、完整进程树和脱离父进程任务治理、原生MCP Tasks协商、新Release和自动合并。任务实例不跨MCP/Actions listener共享。
+In Scope：四个普通工具start_exec_task/get_exec_task/list_exec_tasks/cancel_exec_task，加密持久任务记录、受管进程树、长预算和桌面查询；现有命令引擎复用。
+Out of Scope：应用重启后继续执行/接管进程、交互式stdin/TTY、自动重跑、故意逃逸受管进程组的安全沙箱治理、原生MCP Tasks协商、新Release和自动合并。MCP与Actions命名空间隔离；同一服务在应用内重建可复用任务。
 
 ## 需求列表
 ### FR-1: 快速提交
@@ -21,7 +21,7 @@ Out of Scope：跨应用或服务重启持久恢复、交互式stdin/TTY、自
 **优先级:** Must
 **用户故事:** 作为通过MCP操作本地项目的工程师，我需要幂等重试，以免请求超时导致重复操作或结果误判。
 #### 验收标准（EARS）
-1. WHEN 同一服务实例中request_id和有效命令参数相同 THEN 系统 SHALL 返回同一个job_id；IF 同一request_id参数不同 THEN 系统 SHALL 拒绝重放。
+1. WHEN 同一工作区/服务命名空间中request_id和有效命令参数相同 THEN 系统 SHALL 返回同一个job_id；IF 同一request_id参数不同 THEN 系统 SHALL 拒绝重放。
 
 ### FR-3: 状态与退出码
 **优先级:** Must
@@ -39,7 +39,7 @@ Out of Scope：跨应用或服务重启持久恢复、交互式stdin/TTY、自
 **优先级:** Must
 **用户故事:** 作为通过MCP操作本地项目的工程师，我需要快速取消，以免请求超时导致重复操作或结果误判。
 #### 验收标准（EARS）
-1. WHEN 取消运行任务 THEN 系统 SHALL 先确认cancel_requested并由worker终止直接子进程；IF 已结束 THEN 系统 SHALL 不改写最终结果。
+1. WHEN 取消运行任务 THEN 系统 SHALL 先确认cancel_requested并由worker终止受管进程组或Windows Job Object；IF 已结束 THEN 系统 SHALL 不改写最终结果。
 
 ### FR-6: 断线和可找回性
 **优先级:** Must
@@ -59,11 +59,36 @@ Out of Scope：跨应用或服务重启持久恢复、交互式stdin/TTY、自
 #### 验收标准（EARS）
 1. WHEN MCP或Actions调用 THEN 系统 SHALL 经过唯一call_tool和既有exec_command策略；WHEN 只读profile列出工具 THEN 系统 SHALL 不暴露提交和取消；系统 SHALL 不广告未实现的原生Tasks协议。
 
+
+### FR-9: 安全持久恢复
+**优先级:** Must
+**用户故事:** 作为本地任务操作者，我需要安全持久恢复，使任务可管理且不会误重跑。
+#### 验收标准（EARS）
+1. WHEN 应用重启 THEN 系统 SHALL 恢复加密记录与日志，并把非终态转为interrupted，禁止自动重跑；IF 损坏、缺密钥或命名空间不符 THEN 系统 SHALL 拒绝且保留原文件。
+
+### FR-10: 进程树所有权
+**优先级:** Must
+**用户故事:** 作为本地任务操作者，我需要进程树所有权，使任务可管理且不会误重跑。
+#### 验收标准（EARS）
+1. WHEN 异步子进程启动 THEN 系统 SHALL 建立Unix进程组或在Windows恢复主线程前加入Job Object；WHEN 取消或超时 THEN 系统 SHALL 请求终止整个受管组。
+
+### FR-11: 可配置长预算
+**优先级:** Must
+**用户故事:** 作为本地任务操作者，我需要可配置长预算，使任务可管理且不会误重跑。
+#### 验收标准（EARS）
+1. WHEN 调用者为异步命令提供十分钟以上预算 THEN 系统 SHALL 按工作区上限允许最长24小时；普通exec_command SHALL 仍拒绝超过十分钟。
+
+### FR-12: 桌面任务面板
+**优先级:** Must
+**用户故事:** 作为本地任务操作者，我需要桌面任务面板，使任务可管理且不会误重跑。
+#### 验收标准（EARS）
+1. WHEN 选择工作区服务的异步任务页 THEN 系统 SHALL 显示列表、状态、增量日志和取消；IF 切换工作区或任务 THEN 系统 SHALL 拒绝迟到响应污染新选择。
+
 ## 非功能需求
 NFR-1：查询不等待进程；单次日志每流默认4096、最大16384字节；每流最多保留1MiB。
 NFR-2：新增分页还提供data_base64以精确重组UTF-8边界或二进制。终态结果含output_complete，读取器失败或排空超时明确为false。
-NFR-4：不新增日志落盘，不回传环境变量；沿用路径、命令白名单和confirm校验，不能伪称操作系统沙箱。
-NFR-3：保留旧工具契约；进程时限1至600000毫秒，异步默认600000，不放宽既有10分钟安全上限。
+NFR-4：新增任务记录和日志仅加密落盘，不回传环境变量；沿用路径、命令白名单和confirm校验，不能伪称操作系统沙箱。
+NFR-3：普通exec_command默认30秒/最高10分钟不变。异步默认10分钟，预算受工作区max_task_timeout_ms限制，最高86400000毫秒。
 
 ## 依赖关系
 依赖现有tokio/tauri async runtime、ExecSession、Harness和ToolContext；不新增第三方包。
diff --git a/src-tauri/Cargo.toml b/src-tauri/Cargo.toml
index 33c4be3..d083a99 100644
--- a/src-tauri/Cargo.toml
+++ b/src-tauri/Cargo.toml
@@ -52,6 +52,7 @@ windows = { version = "0.61", features = [
     "Win32_System_Diagnostics_ToolHelp",
     "Win32_System_ProcessStatus",
     "Win32_System_Threading",
+    "Win32_System_JobObjects",
     "Win32_UI_Shell",
     "Win32_UI_Shell_Common",
 ] }
diff --git a/src-tauri/src/actions/listener.rs b/src-tauri/src/actions/listener.rs
index f6f1b5a..613790c 100644
--- a/src-tauri/src/actions/listener.rs
+++ b/src-tauri/src/actions/listener.rs
@@ -131,7 +131,7 @@ async fn serve(
     shutdown: oneshot::Receiver<()>,
 ) -> Result<(), Box<dyn std::error::Error + Send + Sync>> {
     let workspace = tools::Workspace::new(workspace_path.clone()).map_err(|e| e.message())?;
-    let ctx = Arc::new(ToolContext::from_workspace(
+    let mut ctx = ToolContext::from_workspace(
         workspace,
         crate::workspace::AuthConfig {
             auth_type: auth_type.clone(),
@@ -140,7 +140,9 @@ async fn serve(
         policy.clone(),
         "full".into(),
         policy.permission_mode.clone(),
-    ));
+    );
+    ctx.enable_durable_tasks(profile_id, "actions");
+    let ctx = Arc::new(ctx);
     let tools: Vec<Value> = tools::list_tools()
         .into_iter()
         .filter(|tool| {
diff --git a/src-tauri/src/commands/mod.rs b/src-tauri/src/commands/mod.rs
index 5ef4d63..3da7a8b 100644
--- a/src-tauri/src/commands/mod.rs
+++ b/src-tauri/src/commands/mod.rs
@@ -1,3 +1,6 @@
+#[path = "异步任务v2.rs"]
+mod exec_tasks;
+pub use exec_tasks::control_exec_tasks;
 mod app_info;
 mod frp_profiles;
 mod health;
diff --git a/src-tauri/src/commands/异步任务v2.rs b/src-tauri/src/commands/异步任务v2.rs
new file mode 100644
index 0000000..9d0c682
--- /dev/null
+++ b/src-tauri/src/commands/异步任务v2.rs
@@ -0,0 +1,39 @@
+//! Trusted desktop task controls; execution remains confined to the MCP/Actions
+//! dispatcher. The UI can only list, inspect or cancel, never submit a command.
+use serde_json::Value;
+use tauri::State;
+use crate::app_state::AppState;
+use crate::error::{AppError, AppResult};
+use crate::tools::{ToolContext, Workspace, PolicySettings};
+use crate::workspace::WorkspaceProfile;
+
+pub(crate) fn context(profile: &WorkspaceProfile, channel: &str) -> AppResult<ToolContext> {
+    let (policy, auth) = match channel {
+        "mcp" => (PolicySettings::from_runtime(&profile.runtime), profile.auth.clone()),
+        "actions" => (PolicySettings::from_actions_config(&profile.actions), crate::workspace::AuthConfig {
+            auth_type: profile.actions.auth_type.clone(), ..Default::default()
+        }),
+        _ => return Err(AppError::Message("服务通道必须是 mcp 或 actions。".into())),
+    };
+    let workspace = Workspace::new(profile.path.clone().into()).map_err(|e| AppError::Message(e.message()))?;
+    // A local operator may stop jobs even after a remote profile becomes read-only.
+    // The operation whitelist below never exposes arbitrary execution to this IPC.
+    let mut ctx = ToolContext::from_workspace(workspace, auth, policy.clone(), "full".into(), policy.permission_mode.clone());
+    ctx.enable_durable_tasks(&profile.id, channel);
+    ctx.local_task_control = true;
+    Ok(ctx)
+}
+
+#[tauri::command]
+pub async fn control_exec_tasks(state: State<'_, AppState>, id: String, channel: String, action: String, args: Value) -> AppResult<Value> {
+    let tool = match action.as_str() {
+        "list" => "list_exec_tasks", "get" => "get_exec_task", "cancel" => "cancel_exec_task",
+        _ => return Err(AppError::Message("不支持的任务管理操作。".into())),
+    };
+    let profile = state.with_workspaces(|store| store.get(&id).cloned()
+        .ok_or_else(|| AppError::Message("工作区不存在。".into())))?;
+    tauri::async_runtime::spawn_blocking(move || {
+        let ctx = context(&profile, &channel)?;
+        Ok(crate::tools::call_tool(&ctx, tool, &args))
+    }).await.map_err(|_| AppError::Message("任务管理线程异常；请重新查询，不要重跑命令。".into()))?
+}
diff --git a/src-tauri/src/data/mod.rs b/src-tauri/src/data/mod.rs
index ae2be42..d12606f 100644
--- a/src-tauri/src/data/mod.rs
+++ b/src-tauri/src/data/mod.rs
@@ -1,3 +1,6 @@
+#[path = "任务快照v2.rs"]
+mod task_archive;
+pub(crate) use task_archive::TaskArchive;
 #[path = "系统密钥v6.rs"]
 mod key_store;
 #[path = "配置加密v6.rs"]
