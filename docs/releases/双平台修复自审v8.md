# 双平台发布修复自审 v8

## 目标与证据基线

用户要求修复最终发布运行34366631756的Windows脚本兼容超时，并发布Ubuntu和Windows版本。主分支基线cc8f730b98ecc661b75e990cf2e360ee9bdfe42d，源码树fff155c93d802165e5fc441e54c533b4fed39c6f；已从Actions归档逐blob及模式重建核对。旧Windows v0.2.4、历史失败和既有标签不覆盖。

原日志证明测试显式10000ms预算被正确触发（10186ms），不能唯一证明Windows调度、冷启动或杀毒软件是原因。该用例验证脚本兼容性，不是10秒性能基准；此前同源码树通过不保证新机器启动速度。修复测试契约而不改生产执行器默认值或超时上限。

## 自审迭代

评分为执行者自审，非独立审批。正确性40、回归30、来源20、范围10；阈值95且无实际失败。提交前分数仅决定进入CI，任何真实CI失败均打回。

| 阶段 | 分数 | 结论 |
|---|---:|---|
| 原发布失败复核 | 80 | Windows兼容测试误将宿主启动速度当功能契约；发布被阻止，打回 |
| v8初稿审查 | 92 | 仅放宽预算不足：生产yield最多30秒，必须继续读原会话；补轮询与不重复执行检查后再审 |
| v8本地候选 | 96 | 141项Python回归、6FR规格零错误零警告、两工作流YAML和20段Bash语法通过；仅允许实际CI验证 |

## 实质改动

既有Windows兼容用例使用60秒有界预算、原会话轮询、最终退出状态/退出码/精确输出/截断标志断言，不重复提交命令；新增100ms明确TIMEOUT负例与yield=0只执行一次回归。连续20轮真实兼容用例与两个负/保留会话测试成为Windows发布门禁，零个匹配测试不能算通过。Linux专用用例不冒充Windows通过。

统一项目六处版本0.2.6，不改依赖图。Ubuntu原有四组合原生验收完整保留；Windows复用真实NSIS安装、PE版本、精确载荷、窗口及存活验收。新增只读可复用Ubuntu流程和统一发布入口；只有最终发布job拥有contents:write，必须在release/双平台v8分支且每次写入前main仍指向被测SHA。六个附件同版本、同来源、同运行；独立匿名下载验证每个字节数和SHA256。

原Ubuntu发布脚本和通用Windows资产校验被复用；本轮新增编排层要求Windows20轮证据与同运行标识；没有修改已有生产运行函数。新增已公开说明同时列出DEB、AppImage、NSIS及各平台证据ZIP和统一校验和。

## 审查边界

GitNexus修改前目标测试upstream影响LOW：0直接调用者、0业务流程；图谱FTS离线不可用，精确符号分析可用。提交前detect-changes记录由实际CLI另存，不将工具审查指导称作独立第三方审批。工作区的临时规格镜像、图谱、probe状态均不提交。

本容器没有Rust或原生Windows/Ubuntu桌面，141项本地Python测试主要验证编排和失败封闭条件，不代替原生安装/执行证据。当前文件不宣称v0.2.6已合并或发布；运行后评分及最终结果在PR对应SHA审查中回填。

认证范围：Ubuntu22.04/24.04 amd64 X11/Xvfb，AppImage extract-and-run；Windows x64 Actions原生安装。ARM64、macOS、Ubuntu26.04、全部Wayland/GPU/FUSE/Conda组合、完整原生文件选择器及真实公网长期联调不外推。保留预发布和未签名提示。
