# 双平台并行改动合并自审 v9

创建PR #6时发现main在本轮开发期间已合并PR #5，变为2a6d71917c4261fbbee5a7468a598535149a1376。v8基于cc8f730，不能覆盖并行修订。v8原准备作业34372247529只在push时因GITHUB_TOKEN缺workflows权限失败；141项Python和完整源码树检查均通过，随后由已授权GitHub连接器正常提交，未扩大作业权限。

## 冲突处理与审查

PR #5的exec.rs、Windows功能预算回归v7.py、Ubuntu修复自审v7.md全部按原Git blob保留。保留原有30秒功能预算、五类runner各5轮、Python模块10轮、child_process/exit/输出断言及四项源码契约测试。本分支不删除、不削弱这些用例。

本轮新增测试模块改为tools/mod.rs下的cfg(all(test,windows))独立注册，不再改exec.rs。另加有界60秒PowerShell精确输出正例、100ms真实超时负例、yield=0读取原会话且只执行一次回归。20轮兼容门禁执行保留的PR #5原用例，每轮35条真实命令，共700条命令；每次失败立即停止，并非失败重试。新增三项测试另由Windows全量Rust运行，两个负/会话测试也独立再次执行。

v8中的初稿记录保留为历史；最终以本v9整合说明为准。六处版本0.2.6和双平台同运行6附件发布编排不变；没有应用依赖或生产函数改动。

## 评分

v8整合前因为主分支已变更，不满足合并条件，93/100打回。v9保留并行成果后，145项Python（原141+PR #5四项）回归通过，prod执行摘要及PR #5三文件blob吻合，复审96/100，仅准入新的精确SHA CI。任何真实失败仍打回；最终Ubuntu/Windows打包安装与发布证据必须由当前合并源码重建，不用旧SHA证明新版本。
