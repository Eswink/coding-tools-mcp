# 双平台收尾自审 v10：截图只读传输恢复

## 恢复基线与历史故障核对

接续 PR #6，候选 `cc60c720e2a24e55c4117c0a2c1b863ec2cbe43e`、tree `d4e6318738bd81aee94d030fd78af15c6e94f1a5`。双平台运行 34373242638 已完成构建及验收，候选工件 10113519732 的外层 ZIP SHA256 `7e368a45138a80c4b6b861c6a32c6ee0fdf310915365e79028f88b89e151887f` 已下载重算；Windows 332 / Linux 327 项 Rust 通过，四个 Ubuntu 原生组合各 8/8，Windows 20 轮兼容及明确超时、原会话只执行一次均通过。平台测试有重叠，重复回归不累加为独立覆盖数。

不能用这次绿色结果忽略此前未解释的失败。历史运行 34370644155 的 Ubuntu24 AppImage 工件 10112140135：业务已完成 4 组且面板文本断言成功，堆栈停在 `run -> screenshot -> GET /session/.../screenshot`；tauri-driver 报上游 SendRequest connection reset，随后异常处理成功保存失败现场截图。它不是原来的导航问题，也没有证明产品进程永久崩溃。连接重置的底层触发原因仍不能唯一定位；明确缺陷是取证脚本对可重新读取的瞬时截图传输失败没有有界恢复策略。

## 最小修复和边界

仅修改 `NativeSession.screenshot`，对 RemoteDisconnected / ConnectionResetError 的只读 GET 最多尝试三次，间隔 0.25 / 0.5 秒，记录恢复次数和错误类别；第三次仍失败就抛出。HTTP错误、读取超时、非法base64、非PNG、过小PNG均立即失败；原图像校验与全部业务断言保持不变。通用 request/call、点击、IPC、任务提交及取消均不增加重试，不重启应用来绕过失败，不关闭沙箱。

应用版本继续为未发布的 0.2.6；本轮是验收脚本维护，不修改产品运行逻辑、依赖图或安装载荷。保留 PR #5 三个文件和 PR #6 全部双平台改动。辅助只读源码归档运行 34376158997 提供精确 Git bundle，校验后 HEAD/tree 与候选一致；辅助工作流不进入产品 PR。

## 先红后绿、复审与评分

阈值为 95/100 且无阻断；正确性40、回归30、来源20、范围10。分数为执行者自审，不是独立第三方审批。

| 阶段 | 评分 | 决定 |
|---|---:|---|
| 恢复后检查旧候选 | 90（34/26/20/10） | 历史截图故障尚未收尾，打回；不靠再跑一次碰绿发布 |
| 十项单测初稿 | 94（38/26/20/10） | 仅有模拟调用，缺真实HTTP断连复现，打回补测 |
| 增补真实HTTP服务器后的候选 | 97（38/29/20/10） | 本地156项Python全部通过，仅准入新SHA CI |

新增11项回归：首次成功一次读取、一次断连恢复、两次断连第三次成功、永久断连三次后失败、HTTP错误不恢复、读取超时不恢复、非法base64/非PNG/短PNG不恢复、POST IPC/点击不重放、真实本地HTTP服务主动关闭首次GET连接后同一会话第二次GET取得字节。最初10项在修复前1失败2错误，修复后全部通过；补充真实HTTP测试后11项通过。测试夹具不是原生GUI证据，不把模拟PNG当产品截图。

修改前对精确候选重建 GitNexus 1.6.9：screenshot upstream LOW，1直接调用者、1原生验收流程；离线FTS不可用，精确符号图谱可用。原版mcp-probe-kit 4.0.1恢复无本地旧Plan后建立修复计划并逐步回写；指导型工具不冒充独立审批。提交前检查仅允许本脚本、截图回归和本记录三个文件。

## 最终交付条件

新候选须通过双平台完整CI、Windows安装载荷/PE版本/真实窗口和20轮兼容、两个Ubuntu版本两种包的8组原生验收；失败继续打回。之后才合并，并从最终main精确SHA再构建同一版本六附件、公开预发布、匿名下载核对每个大小和SHA256。最终结果和运行后评分回填PR #6及Release，不在此提交前记录冒称已发布。

认证边界仍为 Ubuntu22.04/24.04 amd64 X11/Xvfb、AppImage extract-and-run，以及Windows x64 Actions原生安装启动；不外推完整原生Windows业务交互、ARM64/macOS/Ubuntu26.04、所有Wayland/GPU/FUSE/Conda环境或公网长期运行。Windows安装包未签名，旧v0.2.4保持不变。
