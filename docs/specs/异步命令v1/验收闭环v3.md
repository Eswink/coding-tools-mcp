# 异步任务验收闭环v3

本记录取代任务v1.md中尚未更新的“未开始/未勾选”开发状态；原始规格、任务和历史记录保留。用户本轮明确授权在验证后合并PR并发布构建包，新增发布范围以发布说明v0.2.4.md及合并验收发布v9.yml为准。

## 已合并功能基线

PR #2；源码1e56f7815e49a90c9d3697c081523f86ee82c2d2；tree f31d7eae31081880e259b24bda9ede4a43c377a3；合并main提交848ac970a78c672d1ce5cbeb2587e63bb191f1d1。

当前源码专项运行34241618618全部success。Windows完整Rust254 lib + 75集成 = 329；Linux252 lib + 75集成 = 327；0失败、0忽略。异步专项各39为完整回归子集，面板浏览器12为生产Svelte组件加mock传输/对话框。不能累加平台计数宣称独立用例数。

已核对Linux/Windows ZIP摘要和各8份内部摘要、面板ZIP摘要。Linux归档550文件按原Git模式重建tree与提交精确一致；不声称Windows归档所有原始字节与Git blob相同。

## FR验收映射

| 需求 | 当前实现及真实测试证据 |
|---|---|
| FR-1 快速提交 | 异步命令v1；submit_returns_before_long_command_and_keeps_result_after_context_clone |
| FR-2 幂等 | concurrent_duplicate_submissions_execute_exactly_one_command_within_retention；different_timeout_with_same_key_is_a_conflict |
| FR-3 终态/截止 | nonzero_exit_is_failed_not_transport_failure；process_deadline_is_not_an_http_wait_timeout |
| FR-4 游标/日志 | byte_pages_are_repeatable_and_report_ring_buffer_gaps；unicode_and_binary_pages_can_be_reconstructed_exactly；任务日志回归v2 |
| FR-5 取消 | cancellation_is_acknowledged_quickly_then_reaches_a_terminal_state；cancelling_a_started_process_stops_it_without_automatic_resubmission |
| FR-6 HTTP断线恢复 | mcp_lost_submit_body_is_recovered_from_a_new_http_connection；任务恢复传输v2 |
| FR-7 配额/保留 | quotas_do_not_evict_active_tasks_or_unexpired_idempotency_records；unconfirmed_termination_retains_capacity_and_deduplication_record |
| FR-8 共享策略 | invalid_arguments_and_policy_do_not_register_a_task；read_only_mcp_does_not_expose_submission_or_native_tasks_capability；Actions共享传输测试 |
| FR-9 持久恢复 | queued_record_is_encrypted_and_restart_never_replays_it；completed_results_and_unicode_output_survive_reopen；损坏/未来版本/独占锁回归及原生凭据跨进程组 |
| FR-10 进程归属 | cancellation_terminates_a_real_grandchild_not_only_the_parent，Linux/Windows均通过；不是逃逸沙箱 |
| FR-11 预算隔离 | task_budget_does_not_expand_legacy_exec_or_accept_injected_internal_flags；未做24小时实耗压力 |
| FR-12 面板 | 任务面板浏览器回归v3的12项交互；工作区/通道/任务切换、取消确认、迟到响应和错误可见 |

额外保护：工作区删除/迁移围栏与并发提交只有一个赢家，未确认任务不得被清理后盲目重试；静默进度不反复重写大日志。

## 本轮发布工程范围

产品Rust/TypeScript/Svelte逻辑保持上述基线；仅增加发布准备/门禁、版本递增和当前验收文档。版本准备复用既有版本递增v7，只能修改五文件六处项目版本；依赖图不变，经正常PR合并，不直接推main。

合并后发布分支必须与main SHA一致；验证/安装/公开附件核验形成同一SOURCE_SHA链。既有v0.2.3和草稿不删除、不改写。任一失败都不按发布成功处理。

## 未覆盖/不作声明

研究工作区连接返回Session terminated，当前容器没有Rust/外网；本轮Rust证据来自当前SHA的GitHub Actions，不伪称容器实跑。mcp-probe-kit/GitNexus在本轮不可用，未声称其独立审查成功。PR没有未解决review thread，但也没有独立第三方审批。

真实公网授权联调、用户机器完整桌面交互和长期压力待本机验收。Release为预发布；已实现/自动化通过不等于无缺陷保证。
