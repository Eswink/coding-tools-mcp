# 任务清单：MCP异步命令v1

## 概述
实现FR-1至FR-8，先提供服务实例内可靠的异步命令入口，不重写命令引擎或发布旧版本安装包。

## 交付物清单（Scope-lock）
预计新建9文件（5源码/测试、3规格、1CI），修改8文件（含既有工具目录契约测试）；新函数约25个，必要测试按验收补齐。中文规格是正式来源；工具check_spec仅在临时目录建立字节一致的requirements/design/tasks镜像。

## 任务列表
- [ ] 1.1 创建并校验异步执行规格，锁定恢复与取消边界
  - **证据块**: tools/exec.rs:53 `unwrap_or(30_000)`；exec.rs:326 `spawn_timeout_monitor`；mcp/server.rs:46 `protocolVersion: 2025-06-18`。
  - **涉及文件**: 本目录需求v1.md、设计v1.md、任务v1.md，各200行内。
  - _需求: FR-1 FR-6 FR-8_ ｜ _设计: 技术方案、设计决策_
- [ ] 2.1 实现原子提交与后台worker，严格保留命令策略
  - **证据块**: tools/dispatch.rs:54 `pub fn call_tool` 是唯一共享入口；context.rs:74 `sessions: Arc::new(SessionStore::new())`。
  - **涉及文件**: 异步命令v1.rs <=450；context/mod/dispatch/policy仅接线；既有大函数不扩展执行逻辑。
  - _需求: FR-1 FR-2 FR-3 FR-5 FR-8_ ｜ _设计: 技术方案、API设计_
- [ ] 2.2 实现有界状态、绝对游标、重复读取及取消确认
  - **证据块**: session.rs:14 `SESSION_BUFFER_BYTES=1_048_576`；session.rs `retained_stream_bytes`原本分次取得buffer和total，不是同一快照。
  - **涉及文件**: 异步命令状态v1.rs <=400；session.rs修复同序持锁快照及读取器超时清理。
  - _需求: FR-3 FR-4 FR-5 FR-6 FR-7_ ｜ _设计: 数据模型、设计决策_
- [ ] 2.3 新增工具schema与模型操作说明，不改变协议协商
  - **证据块**: registry.rs:519 `input_schema`；core与read_only工具列表分别管理暴露。
  - **涉及文件**: 异步命令协议v1.rs <=130；registry.rs新schema分发至独立模块；mcp/server.rs仅追加说明。
  - _需求: FR-1 FR-6 FR-8_ ｜ _设计: API设计_
- [ ] 3.1 运行真实命令和传输回归，对照FR逐条验收
  - **证据块**: tests/call_tool_contract.rs:313 已有retained_session_timeout回归；新测试必须补提交响应丢失恢复，不替换旧测试。
  - **涉及文件**: 异步命令回归v1.rs <=450；异步命令传输v1.rs <=350；异步命令验证v1.yml <=130。
  - _需求: FR-1 FR-2 FR-3 FR-4 FR-5 FR-6 FR-7 FR-8_ ｜ _设计: 测试策略_

## 文件变更清单
| 文件组 | 操作 | 行数预算 |
|---|---|---|
| tools/异步命令v1.rs、异步命令状态v1.rs、异步命令协议v1.rs | 新建 | 每个130至450 |
| tools/异步命令回归v1.rs、tools/异步命令传输v1.rs | 新建 | 每个350至450 |
| tools/{context,dispatch,mod,policy,registry,session}.rs、mcp/server.rs | 修改 | 新增逻辑委托独立模块 |
| 三份中文规格与异步命令验证v1.yml | 新建 | 每个200以内 |

## 检查点
阶段1：原版check_spec通过。阶段2：实现范围与影响图一致。阶段3：跨平台检查/真实回归完成，任何失败先修复不降级。

## 需求覆盖矩阵
| 需求ID | 设计章节 | 任务编号 | 状态 |
|---|---|---|---|
| FR-1 | API设计 | 1.1,2.1,2.3,3.1 | 未开始 |
| FR-2 | 数据模型 | 2.1,3.1 | 未开始 |
| FR-3 | 数据模型 | 2.1,2.2,3.1 | 未开始 |
| FR-4 | API设计 | 2.2,3.1 | 未开始 |
| FR-5 | 设计决策 | 2.1,2.2,3.1 | 未开始 |
| FR-6 | 设计决策 | 1.1,2.2,2.3,3.1 | 未开始 |
| FR-7 | 数据模型 | 2.2,3.1 | 未开始 |
| FR-8 | 技术方案 | 1.1,2.1,2.3,3.1 | 未开始 |

范围复核：保留26个既有核心工具，增加4个；既有registry和call_tool_contract数量断言同步为30，新增四个名称逐项检查，不删除旧断言覆盖。


## 第二阶段交付任务
- [ ] 4.1 加密任务记录、每命名空间单密钥、独占锁、故障拒写及interrupted恢复。_需求: FR-2 FR-6 FR-7 FR-9_；设计: 持久任务设计v2。
  - **证据块**: tools/异步命令v1.rs 的 reserve/run/cancel 与 context.rs 的 enable_durable_tasks 接线。
- [ ] 4.2 Unix进程组和Windows暂停/加入Job/恢复，取消和超时清理。_需求: FR-5 FR-10_；设计: 持久任务设计v2。
  - **证据块**: tools/异步命令v1.rs 的 reserve/run/cancel 与 context.rs 的 enable_durable_tasks 接线。
- [ ] 4.3 24小时异步预算与普通exec十分钟策略隔离。_需求: FR-3 FR-8 FR-11_；设计: 持久任务设计v2。
  - **证据块**: tools/异步命令v1.rs 的 reserve/run/cancel 与 context.rs 的 enable_durable_tasks 接线。
- [ ] 4.4 任务面板、IPC只读查询和取消、增量UTF8与迟到响应防护。_需求: FR-4 FR-12_；设计: 持久任务设计v2。
  - **证据块**: tools/异步命令v1.rs 的 reserve/run/cancel 与 context.rs 的 enable_durable_tasks 接线。
- [ ] 4.5 完整双平台Rust/前端回归，review后修复，逐轮记录。_需求: FR-9 FR-10 FR-11 FR-12_；设计: 测试策略。
  - **证据块**: .github/workflows/异步命令验证v1.yml 执行异步专项、完整回归与 -D warnings；tests/任务日志回归v2.test.mjs 运行真实生产函数。

| 需求ID | 设计章节 | 任务编号 | 状态 |
|---|---|---|---|
| FR-9 | 持久任务设计v2/安全恢复 | 4.1,4.5 | 实施中 |
| FR-10 | 持久任务设计v2/进程所有权 | 4.2,4.5 | 实施中 |
| FR-11 | 持久任务设计v2/执行预算 | 4.3,4.5 | 实施中 |
| FR-12 | 持久任务设计v2/桌面查询 | 4.4,4.5 | 实施中 |
