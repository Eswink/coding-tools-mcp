# Iterations 3-8: diagnostic and routing candidate

## Evidence and root-cause boundaries

Iteration 1 verified released source tree `181e5353c38aa39fa72d8b96abedfe1f75e80982`.
Iteration 2 independently reproduced the user's public failure in run
`34691913140`, job `103548547819`, 2026-09-12T11:46:31Z. Its credential-free
artifact `10297219573` SHA-256 is
`6c2e34a5345f5fb2dbc79721c2d6684cd326751cc7b03ca29d76cb9412aaa867`.
The attached JSON is the extracted original, not a synthetic response.

Iteration 3 reviewed the actual listener: all three metadata routes exist, and
unauthenticated POST /mcp emits the correct path-specific challenge. The public
MCP GET identity and 401 challenge both match. This is not evidence that OAuth
is disabled, nor proof that a real account can authorize.

Iteration 4 reviewed FRP config generation and Cloudflare launch paths: no
MCP-only locations are generated by FRP; Named ingress is remote-managed.
The three public failures are identical 146-byte HTML responses with an Nginx
header, while /mcp reaches the application. A selective ingress rule is the
leading explanation. No actual server configuration was read or changed.
Nginx's header alone is neither proof of topology nor a trusted identity.

## Candidate repairs (iterations 5-8)

- Distinguish non-JSON metadata 404 (including a bounded Nginx header hint) from
  OAuth-not-configured JSON, FRP errors, invalid metadata and other statuses.
  A matching string with HTML MIME must not claim the listener disabled OAuth.
- Add fixed-method/validated-origin request text and stable error code to the
  health API. Clear both when the configuration snapshot becomes stale.
- When local metadata and the public 401 challenge pass, explain that the
  public documents remain missing; do not suggest disabling OAuth or rotation.
- Provide five precise Nginx location templates preserving URI, access-control
  review, ACME, dotfile protection and the existing /mcp upstream/Host settings.
  This is a reviewed template, NOT an applied change to the user's gateway.
- Add escaped request display and reusable routing help to the health
  panel. Existing asynchronous invalidation/save/restart logic is unchanged.
- Add opt-in real Nginx tests covering both static prefix and regex shadowing,
  corrected template behavior, ACME/dotfile preservation, metadata identity,
  PKCE, code replay and unapproved-chat denial with fixture credentials only.

## Impact, validation and outstanding gates

Upstream source review: probe::check -> oauth_checks/service_checks ->
run_health_checks command -> HealthPanel; HealthItem has no other Rust literal
constructors. New JSON fields are additive and frontend fields are optional.
The stale-snapshot command clears them, preserving lifecycle isolation.
Risk MEDIUM: diagnostic classification and UI serialization; no change to auth,
principal verification, approval gates, tool execution or tunnel process startup.

GitNexus impact was attempted for modified functions and failed ENOTCACHED;
exact mcp-probe-kit 4.0.1 install failed EAI_AGAIN. Manual review is disclosed as
fallback, not successful impact tooling. Remote source tree must equal staged
local tree before advancing the branch. No main, tag or release change.

Local complete frontend regression: 128 passed, 0 failed/skipped. This includes
four new routing/escaping/template/stale-state contracts. The local container
has no Rust toolchain, Nginx or Windows; Rust failure-first, real Nginx and full
Windows/Linux regressions are PENDING the exact candidate's CI, not PASS.
Public observation remains a separate failing gate, never continue-on-error.
