# 固定公网入口发布收敛评审 v4

## 结论与版本边界

本次实际执行第21—27轮，共7轮（不是新增20轮），每轮有指导工具调用、Agent自审/评分和真实JSON心跳。最终自审 **82/100，request_changes**；原版主计划converge于2026-09-08T04:34:32Z返回 **passed=false / blocked**。评分不是覆盖率、完成度或独立审查者批准。未合并main、未打标签、未发布、未运行安装包构建或macOS Actions，也未操作真实DNS/FRPS/Cloudflare账户。

- 起点：746df552ec3d7821680b49d7e3db7c0ed5eb5ab4。
- 本轮已提交源码：c105ea77a6e25cadc170178af70bc2f63e011674；分支feat/固定公网入口v1，PR #1仍应保持Draft。
- 新增6个提交，18个变更文件。最终从远端工件逐字节核对18/18一致；不是仅检查了本地候选。
- 本轮CI实际checkout PR合并提交de2f32919d49646c872658841b29098f9e82da6d；它与分支HEAD的Git树均为3a67f2b759ad6215c740adb9f40dd096798c0bb6，已通过GitHub元数据核对内容一致。
- 本报告后续若单独入库，为纯文档提交；以下CI证据绑定上述源码，不自动外推未来变更。
- 必需写入被平台安全检查拦截后未重试同一操作或换接口规避。停止原因：`required_write_blocked_and_external_acceptance_missing`；未执行的后续13轮明确跳过，不计为完成。

## Rust基线：历史故障、当前修复与未闭合项

历史失败run34154843356/job101844418201，源码5c45ad8…：store.rs调用私有migrate_data触发E0603。起点746源码已改为pub(super)，且当时普通Rust基线已绿；这不是本轮新增修复。根因是跨文件逐次提交形成中间不一致状态。本轮改用适当的原子多文件提交，并增加全目标检查及生产库-D warnings。

严格检查真实暴露Linux6项、Windows5项既有编译警告。本轮删除两套无调用的内部spawn_listener旧包装及其导出、无调用resolve_frp_server，共消除5项；相关modules为crate内部模块，实际消费者使用with_origin。未修改OAuth issuer/audience验证或工具执行内核。

最终Windows：全目标check、全量249项测试、生产库-D warnings全部通过。最终Linux：全目标check、全量246项测试通过，但-D warnings仍失败，唯一残余是src-tauri/src/commands/ui_memory.rs:47的bytes_to_mb非Windows未使用。候选修复是在函数前增加一行`#[cfg(windows)]`；该写入包含于被拒绝的3文件操作，仍未进入远端。没有用allow(dead_code)、去掉-D warnings、continue-on-error或跳过Linux掩盖问题。

## 逐轮review、评分与修复

| 轮次 | 自审分 | 实际交付/核验 | Review发现及处理 |
|---:|---:|---|---|
| 21 | 82 | 76ce346…新增双平台严格Rust工作流与逐SHA证据 | 区分旧E0603已修复与当前警告；先让严格门真实报错 |
| 22 | 78 | 0d0303a…原子提交MCP/Actions listener及导出4文件 | 0调用旧包装清理；另3文件写入遭拒，保留未决，不规避 |
| 23 | 82 | 32e3b1f…停止态Quick清空、Named保留固定配置、新增3回归 | 修复上一批STATE-01；测试不冒充真实Cloudflare联调 |
| 24 | 88 | 29ca567…3份中文规格、字节一致协议镜像、原版校验与9回归 | 不仅是中文名兼容：原文缺需求/设计/任务结构；补齐12FR和覆盖矩阵，任务不虚假勾选 |
| 25 | 85 | 76265a7…发布标签/SHA/六版本/脏源码门与14本地回归 | 先以脏源码负例得到13过1败，再修至14过；远端进一步发现真实锁文件版本漂移 |
| 26 | 86 | c105ea7…只同步锁文件两个项目版本并保存失败日志 | 0.1.28→既有0.2.1，154个依赖节点/解析地址/摘要不变；双平台来源CI各14通过 |
| 27 | 82 | 最终远端18文件、补丁及证据核验；正式收敛拒绝 | 修正Ralph控制进度未同步、图谱范围误读；保留严格Rust、UI、安全、实机与架构门未决 |

code_review提供指导，判断来自当前执行Agent，不存在独立第二位审查者审批。早期轮次的pending/cancelled测试未被追认为当时已通过；最终版本验证单独列示。

## 最终真实检查

| 检查 | 结果 | 原始证据 |
|---|---|---|
| 前端npm ci/check/build/入口回归 | 全部success | run34186837731/frontend |
| 普通Rust Linux/Windows check/test | 全部success | run34186837731两个Rust jobs |
| 严格Windows全目标check/test/库-D warnings | success，249测试通过，0失败 | run34186837766/job101936778613 |
| 严格Linux全目标check/test | success，246测试通过，0失败 | run34186837766/job101936778419 |
| 严格Linux库-D warnings | failure，bytes_to_mb，退出101 | 同run的Linux严格编译日志 |
| 发布来源Linux/Windows | 各14项真实临时Git回归通过，六处0.2.1 | run34186837881/jobs101936778877、101936779031 |
| 原版规格check_spec 4.0.1 | 12FR，0error，0warning，9正反例通过 | run34184900684已下载原始工件；最终远端源码独立重跑同9项 |
| 下载最终远端源码后独立入口回归 | 47通过、0失败、0跳过 | 第27轮远端入口回归v4.txt |
| 下载最终源码后独立版本/规格回归 | 14/9通过 | 第27轮远端版本回归v4.txt、规格回归v4.txt |
| 18个变更文件字节核对 | 18/18一致 | 第27轮最终交付18文件核验v4.json |
| 未提交5文件补丁 | 隔离目录apply/check/逐字节/反向还原均退出0 | 第27轮待提交补丁核验v4.json；不代表候选Rust已编译 |

Linux246=171单测+75集成；Windows249=174单测+75集成。平台专属测试不同，不能把249与246相加当成独立覆盖数量。本地无Rust编译器，Rust结果来自真实GitHub Actions。新的两项迁移可见性回归尚未提交，不计入上述通过数量。

## 规格与发布链修复边界

中文需求/设计/任务是权威原文，临时镜像仅把相同字节映射到原版工具要求的ASCII目录和requirements.md/design.md/tasks.md。固定原版4.0.1、重读摘要一致、正例及缺FR/SHALL/覆盖/章节/占位等反例校验；没有修改检查器。规格子计划已converged，但这只关闭SPEC-01结构问题，不能替代产品验收。

发布流程此前手动release_tag与默认checkout分离，会错标来源；现按现有tag解析出的不可变SHA构建，校验六处版本、标签和干净的已跟踪源码，再生成安装包摘要。构建仅contents:read，发布阶段单独write。标签推送仅构建Windows，公开发布必须手动publish=true；macOS仅独立workflow_dispatch。此行为变化已在执行时告知，并非本轮实际发布。

版本门捕获并修复package-lock根版本与根包版本仍为0.1.28的历史漂移；其余四处本就是0.2.1。本次只是项目元数据对齐，不是新安装包版本升级。今后实际构建前仍须先递增并提交项目版本。版本/文件摘要不是二进制签名、安全审核或实际安装验证。当前macOS工件改为DMG+来源JSON，未额外上传裸.app目录，需要产品验收确认；本轮未运行macOS来验证它。

## 剩余发布阻断

1. RUST-02/WRITE-02：Linux严格编译的一行修复、data/mod.rs、迁移可见性回归v4.rs仍在本地。必要写入未获执行，不能宣称Rust全部修复。
2. UI-01：上一批TunnelConfigForm.svelte与workspace/[id]/+page.svelte尚未提交，Actions Quick复制链路未闭合。旧三文件v3补丁含已被本轮取代的supervisor，不能整包重复应用；v4补丁已经排除它。
3. SEC-01：凭据仍JSON明文。Unix0600、原子写、文件锁和环境变量不是加密；系统安全存储、Windows权限、迁移与canary实测仍待完成。
4. E2E-01：真实FRPS/Cloudflare/DNS/TLS/ChatGPT授权/重启、断网恢复与安装包验收缺证据。本地HTTP或运行态fixture不能替代。
5. MAINT-01/RELEASE-03：既有长文件与完整事务仍待整理；ARC-8 drift因非结构化基线及未闭合证据返回blocked(arc-2/7/8)。工作流仅静态检查和版本回归，NSIS/DMG未实际构建。

## 过程证据修正

最终GitNexus compare输出21文件/75符号/31流程、critical，实际范围为18个已交付文件+当前工作树3个未提交Rust候选；独立保存的旧UI2文件不在该图谱。FTS扩展离线缺失，关键词搜索明确degraded；使用精确符号影响分析及实际源码。通过每次调用的core.quotepath=false修复中文路径漏报，没有修改生产全局Git配置。

最终review发现.ralph/PROGRESS.md控制进度实际仍为初始化状态，虽然逐轮JSON心跳、评分及diff均存在；第27轮依据原始记录补齐控制进度，没有声称此前每轮都已同步该控制文件。首次最终converge因使用错的本地常量ID报参数错误；改用已成功心跳的实际plan_id后原版工具正式返回blocked。两个原始结果均保留，未将工具错误记成PASS。

## 下一检查点

从最新分支恢复后，先通过正常受审流程完成5个待提交文件的集成（v4补丁只做过隔离应用校验），复跑双平台严格Rust与界面回归；然后完成凭据安全和真实公网/安装包验收。任何高风险未决项尚在时继续保持Draft，不自动发布，不在后台循环。
