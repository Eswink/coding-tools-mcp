# 聊天授权迭代 v8：Windows 原生窗口验收

## v7 实际结果与复审
`fa5f8538e04c10b3b7b707c1b915879a6090d916` 对应 Actions `34437558523`：两端 Rust 全部测试与生产零警告、两端前端、Ubuntu22/24 八组原生验证成功。v7 认证修复本轮复审 97/100（功能30、回归29、兼容19、可追溯19）。本评分只针对该聚焦修复，不代表整个发布已达标。

## v8 范围
复用八组真实审批行为，在 Windows 原生 WebView2/Edge WebDriver 中执行。驱动版本从宿主机 WebView2 文件读取，只从 Microsoft 官方精确版本路径下载并记录摘要，不回退不匹配驱动；原生 capability 是 webview2 + ms:edgeOptions.binary，而非普通 Chromium 或模拟组件。批准/拒绝/撤销始终是 WebDriver 实际点击，不直接注入审批 IPC。子进程清理仅针对当前调用创建的驱动 PID。Windows 使用全新 GitHub 托管机实际原生凭据存储，拒绝覆盖已有应用配置；工作区位于 RUNNER_TEMP 有标记的专用目录。

现有 Linux 仍保持普通用户/Xvfb/D-Bus/Secret Service。共同 harness 增加版本与空工作区断言，Python 命令使用平台可用名称。测试适配器与夹具边界三项、本来 harness 四项单测本地通过；它们是单元测试，不冒充 Windows 原生实测。增加安装包种类标注供后续安装验收使用，当前工作流仍只编译 debug 程序，不输出正式 Release。

## 影响及评分
修改前 GitNexus 对 run 符号上游影响 1 个直接入口，LOW；动态 CI 与两原生后端人工复核。候选自审 91/100：Windows 原生实测尚未执行，打回最终交付并进入 CI 修复验证。可信公网入口/真实 ChatGPT 双窗口、DEB/AppImage/NSIS 安装载荷内的完整审批验证仍为未决项，不能被该分数替代。

依据：https://learn.microsoft.com/en-us/microsoft-edge/webview2/how-to/webdriver
Selenium 官方 EdgeOptions 原码确认 UseWebView 对应 browserName=webview2。
