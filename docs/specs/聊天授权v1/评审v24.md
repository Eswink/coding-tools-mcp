# 迭代 v24：Windows 短路径断言修复

v23 实际运行 34493849022 / job 102927279377：原生令牌/交互窗口 smoke 成功；辅助回归在标准用户路径测试失败，因 tempfile 返回 RUNNER~1 而被测 inside() 正确规范化为 runneradmin。这是期望路径未规范化，不是权限校验失败。v23 运行后自审 85/100，打回；没有进入业务审批，不记为原生通过。

仅将该测试临时根规范化后比较；原先拒绝父目录/邻居和新增八项边界断言均保留。Windows 预检在运行测试前即保存源码身份和日志，避免测试早失败时证据目录为空。保留 pipefail，任一测试失败仍使 CI 失败。

本地64项辅助回归通过，diff与Python/YAML检查通过。图谱对新增测试符号未命中，不能声称有完整静态调用图；人工确认调用仅 unittest，产品权限判断零改动。执行者自审94/100，仅提交候选准入CI；标准账户真实原生、NSIS及整个发布仍未放行。
