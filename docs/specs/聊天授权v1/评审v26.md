# 迭代 v26：明确验收例外的双平台预发布

基线a94d9f16336bcb0222b02862abfd11c7c48332bc；v25修复外层API标志冲突，68项辅助回归通过。正式发布不直接沿用功能分支旧二进制：正常审核合并后，建立明确发布分支并锁定与main相同的精确提交，从该提交重新执行双平台Rust/HTTP、前端、Ubuntu原生和安装矩阵、Windows构建安装及资产门禁。

用户在本轮明确授权Windows原生自动化持续受阻时由本人本地核验。默认严格模式不变，仅refs/heads/release/聊天授权本地验收v26能启用该例外。它不将失败变成通过：Windows完整审批显示pending_local_verification、stages=0，真实安装、精确NSIS载荷、版本和可见窗口持续存活仍是不可豁免条件；普通feature运行依旧执行严格原生测试。真正ChatGPT双窗口及公网来源验证两种模式都标为未验证。

新增发布聚合器拒绝缺失/篡改安装包、源码/tree/run/版本错配、四组Ubuntu缺任何一组、失败/重复原生阶段、Rust失败/忽略/不完整、伪造真实ChatGPT、未经授权的本地例外和主分支并发变化。复用既有经过测试的GitHub发布助手：先draft逐项核验，再公开prerelease，不覆盖已有不同内容资产；最后匿名重新下载六附件核验。中文文件保留在本地与证据包，GitHub传输资产使用既有ASCII名称约束避免规范化冲突。

初审89/100，发现需限制例外入口为精确发布ref并在公开元数据明确与完整原生PASS不同，修正后本地自审95/100（可维护性28/30，权限边界25/25，负向回归22/25，追溯20/20）。19项新发布负向回归、68项授权辅助、20项既有发布回归通过；YAML解析与Bash静态语法通过。此评分仅准入CI，不是对尚未运行的发布或Windows原生验收打分。

本轮不修改产品源码、权限策略、依赖、版本、系统沙箱或桌面DACL；新发布权限只授予最终publish job，所有构建/测试job仍只读。新增本地核验指南要求用户核对A批准/B拒绝、任务归属、撤销、独占、只读及重启重新审批，并声明exec.run不是OS沙箱、会话元数据不是密码学认证身份。

## v25 实测结论与例外依据

实际验证运行34501403840、工件10162685682（ZIP SHA256 3ec82e5675dbe643bc1e808436c89298655cc9eb79f7659402b04a8810a93c3d）已下载重算。外层WinError 87消失，标准账户已创建并通过令牌校验；等待验收进程480秒后超时，无八阶段结果。owned_processes_closed/account_deleted/profile_deleted全部true，credentials_scan_completed=true、credentials_found=false，sandbox_disabled=false。这是宿主测试执行阻断，不是已观测到业务越权，不能称完整Windows PASS。v25局部API修复保留，整体验收85/100打回，按用户本轮授权采用显式本地核验预发布路径。

提交前图谱重建后检测8文件54符号18流程，聚合风险CRITICAL（新发布入口与回归调用集中）；已向用户告知，未忽略。compose上游两个直接调用为新main与测试适配方法，没有产品Rust调用方。源码运行图的FTS不可用，动态YAML入口和权限由人工核对。该变化仅新增可审计发布能力，授权生产逻辑不变；最终仍须Actions实际执行、正常PR合并、精确main构建和发布后六附件匿名校验。
