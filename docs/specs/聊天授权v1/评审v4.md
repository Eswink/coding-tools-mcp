# 聊天授权迭代 v4：恢复真实基准与修复验证入口

## 基准校准
远端功能分支为 `42cf7b285458b64135172ae3a196a7ab2207850c`，源码树为 `b2e9c36892884b3f7b9a0ac0373a515b49fe3589`。已下载 Actions 34390085367 的源码工件 10119259339，校验外层 SHA-256 并重建相同 Git 树。上一轮摘要中的 `3814d8c` 尚未在远端分支确认，不能计为已提交。

## 本次范围
- 修复 lib.rs 对本机审批 IPC 的缺失导入，不修改审批策略。
- 新增可跨平台直接执行的前端回归入口，真实编译三份 TypeScript 源码到独立临时目录，运行全部 `.test.mjs` 和 `.test.cjs`，不依赖历史 `/tmp` 文件，不删断言。
- CI 移除临时补丁解码/提交/推送逻辑，全流程 `contents: read`，所有 job 检查同一明确源码 SHA。
- 前端也加入 Windows/Ubuntu 矩阵；保留 Rust `-D warnings` 与全部测试，不以失败重试取代修复。

## 实际验证
原版 mcp-probe-kit 4.0.1 check_spec 对既有中文规格逐字镜像校验：6 条 FR，0 error、0 warning。GitNexus 1.6.9 索引 5,307 节点、13,041 边；全文搜索扩展离线不可用，符号调用图可用。已调用入口影响分析和变更检测；导入与工作流调整不是已索引函数体改动，图谱零命中不等于完整安全审查。

本地 Node 语法检查、7 项界面/提示词源级回归、工作流 YAML 与只读权限断言通过。容器没有 Rust 工具链与产品 npm 依赖，不宣称完整编译通过；必须读取本次提交的 CI 实际结果。

## 语义复审
认证与授权仍属 CRITICAL 变更。本轮只解除基础编译/测试准备阻断，未执行原生审批或真实 ChatGPT 双窗口验收。当前 OAuth 仅支持授权码，不存在刷新令牌；初始化说明已是静态文本，不能复用前轮不匹配的“刷新/初始化修复”描述。下一轮审查 OAuth resource 显式绑定、scope 严格校验与负向用例。

执行者自审：80/100，允许进入修复验证 CI；整体交付未达标，禁止合并/发布。该分数不是独立安全审批。
