# Windows 原生 v29：交互桌面访问的可撤销会话授权

v28真实运行34510183374、工件10165688438，SHA256 001a76fbcf24dcdbea516ed2f724980bc76d1748dd9f18b672cba6ee20da623f：标准宿主成功进入bootstrap，在import ctypes时出现“DLL initialization routine failed”，并非找不到DLL；未进入child、更未访问应用/OAuth。v28异常证据修复成立但整体原生未过，实际自审82/100打回。

## 依据及竞争假设

CPython官方提交0e8100a140414ceb9086a1d5d7dd24209f111d41明确_ctypes原本加载ole32后加载user32。固定Python3.12.10不含其2026年8月main上的延迟加载修改，不盲目升级掩盖桌面权限问题。微软说明新LogonUser会话使用自己的logon SID，交互进程需要窗口站和桌面的访问权；显式指定lpDesktop时必须由调用者保证。NULL桌面隐式继承并不证明实际WinSta0/Default可访问。

https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-createprocesswithtokenw
https://learn.microsoft.com/en-us/windows/win32/winstation/process-connection-to-a-window-station
https://learn.microsoft.com/en-us/windows/win32/winstation/window-station-security-and-access-rights
https://learn.microsoft.com/en-us/windows/win32/winstation/desktop-security-and-access-rights
https://github.com/python/cpython/commit/0e8100a140414ceb9086a1d5d7dd24209f111d41

## 最小修复与风险约束

只允许本仓库github-hosted临时Windows。取真实TokenLogonSid，必须是唯一S-1-5-5-X-Y，并且原ACL尚无该SID。只给WinSta0/Default追加无继承的有限GUI ACE（无WRITE_DAC、WRITE_OWNER、DELETE、通用全权；桌面无hook/journal），读取有效AccessCheck前后和ACL回读。显式winsta0\default与内层一致。流程结束先杀本次Job，再只移除本次精确ACE，保留一切其它/并发新增ACE；回读不一致或清理失败保持失败。产品、普通用户系统、注册表策略、旧发行包未改变；不关闭沙箱。安全影响因涉及GUI访问主动标为HIGH并已提醒，调用图仅run上游LOW。

开启依赖cache-on-failure只避免GUI失败后重复编译依赖，不减少精确源码构建或任何测试。现有68+4+2与新增8行为/ACL测试共82项通过；包括ACE精确清除、并发无关ACE保留、禁止扩大权限、拒绝非CI、清理失败仍失败。Python编译和diff检查通过。执行者候选自审94/100仅准入真实CI；只有Windows实际8阶段结果才可宣称完整修复，真实ChatGPT账号/公网联调独立留本地。
