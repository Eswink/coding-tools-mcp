# 聊天授权迭代 v16：启动失败同机对照

基准为远端9d23813da7cb8570cf8a0ff0a6f7ca0404a5024d，源码工件10139069644外层SHA256 ccabce9b5794803d4609c9bd6dc89beb85c00b0a53f4331464480e418ada7875及重建树423a76c4d1a3e3688b94c3b9e8e742bffe27e1c9均已逐字核验。

v15失败证据10139242809（摘要0c255772b43c76fbc89da220753145e38845ad8f21aea471658d6b6371ffef06）：中等完整性已验证，宿主在运行Python代码前退出0xc0000142，审批0/8。完整job102765803750却证明Bash构建阶段的同一降权启动器和真实USER32窗口冒烟成功。两者之间还有shell、导入和WebView2环境差异；“所有降权进程均无桌面权限”不是已证实根因。v15运行后评分80/100，Windows硬门禁失败打回。

本轮只加独立的只读诊断workflow及新脚本，同一个Windows托管机内先Bash后PowerShell，分别测试原环境、加载适配器后、WebView2子进程环境以及宿主--help四种情况。每个进程仅执行一次，私有Job回收，记录确切退出码和实际令牌，不采集密钥/全环境/其他进程；任意失败保持job失败。该流程明确不是产品验收，不替代两条完整验证/安装门禁，不更改产品或操作系统权限。

GitNexus已重建5538节点/13482边，launch及WindowsNativeSession.__init__上游静态LOW、零直接命中；动态import调用关系人工复核，不能把图谱零命中当作没有影响。本轮不修改已有符号。本地Python编译、非Windows拒绝边界与YAML只读/精确源码/双shell检查通过，真实结果待CI。

自审84/100（定位24/30，安全25/25，可追溯20/20，实证15/25），仅诊断候选；未达发布阈值95，整体交付打回。接续必须按该对照的实测结果作最小修复，不能通过累计高分放行。
