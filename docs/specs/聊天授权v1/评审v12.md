# 聊天授权迭代 v12：原生测试应用降权启动

## v10实证与打回
Actions34441207699仍有Windows失败，工件10138138605摘要8165e2c4c05fafc55c5b7fce92cbe7d1f1f7c1bbb60624617580b9c3b1a17ce3已核验。实际WebView2子进程使用独立user-data目录，但没有remote-debugging参数和监听端口，审批0/8；清理又遇文件句柄占用。v10运行后评分76/100打回，独立缓存假设未解决启动。

Microsoft 2026-07-16安全文档明确：提升权限的WebView2忽略环境调试参数，推荐非提升权限宿主。此次对照症状后增加实际令牌验证，不修改产品浏览器参数来绕过该保护。

## 修复
新增仅托管CI允许调用的Windows原生启动器：从本次调用者令牌创建LUA且删除特权的受限令牌，设置中等完整性，再读取实际子进程令牌确认非提升后才恢复执行。应用先挂起创建、加入本调用私有的kill-on-close Job Object，后代无论根进程是否先退出均由内核对象清理；不依赖复用磁盘PID，不创建账户、不改注册表或系统策略。SANDBOX_INERT不启用，WebView2沙箱不关闭。

程序源码/安装字节完全不改；原生适配器仍是loopback真实WebView2附加。仅子进程具有调试环境；把父/子令牌级别作为无敏感信息证据输出。增加Windows原生Python子进程令牌冒烟，创建/等待/回收失败直接阻断，不跳过或降级。清理等待私有缓存句柄释放有界10秒，构造失败保留首个原始异常，不被附加清理异常覆盖。

## 审查
GitNexus对既有适配器__init__/stop_owned_process/close上游分析LOW，最大1个直接调用、2个传递影响；纯测试平台适配器，无产品调用图变更。新增Win32函数使用明确64位ABI签名和结构尺寸校验，令牌/线程/SID/Job/进程句柄均有错误路径回收；中等完整性不是OS租户沙箱声明。

本地4项新契约单测+6项适配器+4项共同harness通过，Python编译通过。Windows真实令牌冒烟及实际原生审批尚须CI执行，Linux契约测试没有计为原生通过。v11安装矩阵仍在进行，未提前计为成功。

自审90/100，仅准入CI；未达到最终95且零阻断标准，不合并、不发布。下一步核对父/子权限及真实八组审批，再核对所有安装包矩阵。

依据：https://learn.microsoft.com/en-us/microsoft-edge/webview2/concepts/security
https://learn.microsoft.com/en-us/windows/win32/api/securitybaseapi/nf-securitybaseapi-createrestrictedtoken
https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/nf-processthreadsapi-createprocessasuserw
