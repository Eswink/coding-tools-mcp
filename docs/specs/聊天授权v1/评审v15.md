# 聊天授权迭代 v15：绑定原生交互桌面

v13运行34443351588，工件10138894856摘要9fb70320d61a8c45d619532156d1fb54a34d608a9c3a2020362a4e2cd7dbb808已复核。降权后的宿主退出码明确为0xc0000142，尚未运行宿主Python代码，故没有生成应用stdout日志和退出记录，原生审批0/8。诊断不再猜测是OAuth或前端问题。v13运行后自审80/100，打回。

Microsoft CreateProcessAsUser文档规定默认非交互窗口站，GUI需要显式指定winsta0\default；本轮只将该桌面写入测试启动器STARTUPINFO，不改系统桌面DACL或产品代码。强化原生smoke：中等完整性子进程必须真实加载USER32，读取当前站/桌面名，实际创建、显示、更新并销毁一个Windows窗口；仍保留实际子令牌级别/私有Job校验。只启动Python并退出的旧smoke不再作为GUI环境可用证据。

修改前GitNexus对launch上游风险LOW，静态图未解析动态导入，调用方原生适配器由人工复核。新增GUI smoke在非一次性Windows CI直接拒绝，不跳过；本地原有16项纯契约和Python编译通过，真实窗口必须由CI执行。

另已核验版本准备bot提交316f76aab2e38f460072b3f784c96e48d78c28c6，只改五文件六版本为0.3.0，tree14fb4b069b3e0bb4f2902f4490df31dad58b67e6与本地相同，依赖未改。当前提交基于该新版本重新触发正常CI。

自审90/100，仅准入实际Windows诊断及v0.3.0双平台完整验证，不能宣称原生/发布完成。若smoke失败继续按实际错误修复，不关闭沙箱、不篡改安装字节。
依据：https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/nf-processthreadsapi-createprocessasuserw
