> 本轮本地独立迭代记录，原基线为远端 v15；文件另存以保留远端同号评审。后续对齐结论见本地补充评审v19.md。

# 聊天授权评审 v16：失败证据与有界原生就绪检查

## 精确基线

远端 `9d23813da7cb8570cf8a0ff0a6f7ca0404a5024d`，内容树 `423a76c4d1a3e3688b94c3b9e8e742bffe27e1c9`。下载 Actions `34444310441` 的源码工件 `10139069644` 并复算 SHA-256 `ccabce9b5794803d4609c9bd6dc89beb85c00b0a53f4331464480e418ada7875`；从归档重建相同 Git 树。本轮本地提交不是 GitHub 已提交状态。

## 实际发现

Windows 原生工件 `10139242809`：审批 0/8，外层 Python 宿主退出 `0xc0000142`，却被通用轮询吞为 60 秒超时。报告在任何窗口、IPC、OAuth 交互之前预填三项真实验证标记为 true。安装包工件 `10139335961` 同样 0/8；载荷字节核验成功不等于原生授权成功。

进一步审查：finally 中清理失败会覆盖第一次异常，并使结果 JSON 不落盘；读取不存在的二进制也发生在 try 之外，不能产生失败报告。

## 修复范围

仅改变测试 harness：原生就绪只重试连接建立等只读瞬态错误，明确退出和协议错误立即失败；期限校验拒绝 NaN、无穷、布尔和无界数值。把退出进程准确标记为 owned-python-host，记录数值退出码，不误称已证明产品崩溃。三个真实验证标记默认 false，分别在实际阶段之后更新；清理错误强制失败，但不覆盖第一错误，最终报告仍写出。没有放宽产品授权、超时、安全令牌、沙箱或安装包门禁。

## 红绿与自审

新增 8 项初版回归在原实现退出码为 1：复现错误的验证标记、失败证据缺失，并建立就绪 API 的新行为契约。初次修复 8/8；审查发现还须覆盖“成功后清理失败”及宿主退出归因，补齐为 11/11。原有中等权限/适配器/原生逻辑/宿主共 16 项回归仍通过。上述是 Python 故障注入/契约测试，不是 Windows 或 Ubuntu 原生 GUI 新运行。

初审 90/100，打回补充清理与退出归因；本地复审 96/100（功能 29/30、回归 29/30、兼容 19/20、追溯 19/20），仅对应本轮测试证据修复。整个发布目标仍 BLOCKED：没有真实新 Windows CI、没有本轮远端写入接口、没有真实 ChatGPT 双窗口验收；不能用本地分数放行。

GitNexus 对 run/debug_status/__init__ 的修改前影响分析为 LOW；动态 import 和 CI 调用人工复核，FTS 扩展离线不可用如实记录。提交前按真实 diff 执行变更检测。
