# Windows 原生验收恢复 v27

基线是已发布 v0.3.0 的 216b0e10，产品内容树 dc44bac。v25 原始工件 10162685682 / SHA256 3ec82e5675dbe643bc1e808436c89298655cc9eb79f7659402b04a8810a93c3d 表明 WithTokenW 参数87已经消失，但正常标准账户宿主480秒超时，未写业务阶段文件。不能解释成 ChatGPT 登录等待：本机测试 OAuth 使用 NoRedirect，回调只解析而不访问真实ChatGPT。

## 本轮假设与最小候选

外层控制台初始化仍是竞争假设，不先宣称因果成立。用同安装目录 pythonw.exe 启动测试宿主（不是修改产品exe），保留 LOGON_WITH_PROFILE、SUSPENDED、Unicode、标准账户、Job、账号/配置清理和凭据扫描。增加在导入测试代码前的日志与45秒纯堆栈诊断；进入业务验收前取消该诊断。恢复线程必须返回此前挂起计数1，不进行猜测性多次恢复。证据纳入原有凭据扫描再导出。

## 可能性与证伪

1. 控制台/宿主初始化：Pythonw和早期日志判断。失败仍保留原生硬门禁。
2. 账户、桌面、DLL权限：通过早期阶段和Win32返回进一步定位，不改全局ACL/沙箱。
3. WebView2环境变量：微软说明高完整性进程忽略环境覆盖；本轮继续真实正常标准账户，不能用提升权限mock替代。
4. WebView2/CDP与驱动：原生入口继续检查回环监听归属和匹配版本。
5. UI/OAuth/异步任务代码：八阶段完成前不得判定通过。
6. 真实ChatGPT账号/元数据联调：只有这一项由用户本地验证，不豁免本机OAuth。

官方依据：
https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-createprocesswithtokenw
https://learn.microsoft.com/en-us/microsoft-edge/webview2/concepts/security

## 自审

GitNexus run/child 上游分析LOW；run的grant边是subprocess.run名称碰撞，人工剔除，实际入口为main和原生/NSIS工作流。产品、依赖、版本均未改。68项既有辅助回归与4项新增引导契约通过；这不是Windows实际成功。执行者自审92/100，仅准入真实CI；未达到完整交付门禁，禁止发布或改变旧包。
