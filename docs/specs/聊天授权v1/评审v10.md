# 聊天授权迭代 v10：Windows 原生启动诊断与独立浏览器缓存

## 上轮实证与打回
v9 提交 a5030ffff2295e4f539700b8d53fbd615af8797d，Actions 34440092308：两平台 Rust、前端及 Ubuntu22/24 原生均通过；Windows 原生未通过，8组断言执行数为0。工件10137721174外层SHA-256 fa873f2884ee34ff1e8a777e0b97e3bb582c17523300d4c5803ce5be96abbf2b 已重新计算。应用和WebView2子进程已创建，但等待回环调试端口被拒绝连接。v9运行后自审72/100，打回，不能据构建成功宣称原生通过。

## 本轮变更与假设
仅修改Windows测试适配器及其回归。每次启动分配RUNNER_TEMP下独占WebView2缓存目录，通过子进程环境设置WEBVIEW2_USER_DATA_FOLDER，并使用Microsoft文档中的最小remote-debugging-port参数。结束后清理本次目录；不改产品逻辑、注册表、授权策略或全局环境，不关闭沙箱。共享浏览器状态是待验证假设，并非已证实根因。

新增有界原生诊断：仅输出本次Popen进程及后代的名称/PID、调试端口参数是否传递及其监听地址，不输出完整命令行、环境、浏览器数据或OAuth凭据。在创建任何测试OAuth凭据之前验证调试端口属于本应用进程树且只监听回环；缺失、公网监听或其他进程占用均拒绝。失败时保留脱敏诊断，不把缺证据算通过。

## 验证与审查
修改前更新GitNexus图谱，对webview_environment、__init__、close及两个改动测试执行上游分析，LOW，最大1个直接调用；动态调用和进程生命周期另以diff人工复核。本地6项适配器单测、4项共用harness单测、Python编译与git diff --check通过。增加缓存路径、环境隔离、端口归属/回环拒绝用例；这些只是单元测试，不是Windows原生实测。

候选自审88/100：新增诊断足以继续定位，但不能提前确认修复完成。当前拒绝最终验收与发布；必须读取该精确提交的Windows原生实证。安装包载荷审批与真实ChatGPT双聊天/可信入口仍未验收。

依据：Microsoft WebView2 WebDriver attach文档；Playwright官方WebView2隔离用户目录说明。
