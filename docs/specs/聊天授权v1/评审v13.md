# 聊天授权迭代 v13：Windows 应用提前退出取证

v12 Actions34442380800未通过Windows原生，工件10138544680摘要8081c4b760a3df7f3b6376473797870e2c9427e5b3b3a66d678211902ae42dd2已下载核验。父进程实际elevated=true/IL12288，受限子进程elevated=false/IL8192，私有Job创建成功；但应用在WebView2就绪前退出，8组仍为0。故降权是已验证事实，不等于窗口可用；v12运行后自审78/100，打回。

本轮仅改变测试宿主：以已验证的同用户中等完整性Python进程在同一私有Job中创建精确应用，给应用提供有效stdout/stderr/stdin句柄，原始应用退出码及十六进制码保存为独立证据。应用不改任何字节；不创建用户、不变更注册表/系统策略、不允许沙箱旁路，不重放业务操作。所有后代由已有Job回收。退出码显示仍是失败，而非接受提前退出。

修改前GitNexus对适配器__init__/debug_status影响分析LOW（最大1直接调用）；调用图离线FTS不可用，动态宿主进程另以实际diff审查。本地新增2项标准句柄/原始退出码与非CI拒绝测试，加已有4+6+4项契约共16项通过；Python编译/git diff --check通过。没有把Linux契约测试当成Windows实测。

自审88/100，只准入诊断CI。安装包矩阵仍待本次真实结果；未达最终95且零阻断门禁，不合并、不发布。另行准备的交付汇总代码尚未纳入本提交。
