# 聊天授权迭代 v14：独立候选版本准备

实际安装验收已确认：1b3b6b1对应安装流程34442380379四个Ubuntu组合(22.04/24.04×DEB/AppImage)都完整通过；Windows NSIS真实安装和精确字节核验通过，但窗口审批仍0/8。工件10138604522摘要312cf00f792090f6ee55959634c9270766eaabeab5dc13c2746b3722e2dac2b9已下载复核。降权后NSIS WebView2已收到调试参数，但未监听，不能当作Windows验收完成。v13诊断仍在执行。

为了不让新聊天授权候选与旧发行版v0.2.6重名，本轮增加只允许功能分支执行的独立版本准备job。使用既有受限版本脚本，仅递增package.json、package-lock.json、Cargo.toml、Cargo.lock、tauri.conf.json五文件六处为0.3.0；逐份验证其余JSON/TOML依赖图和字段不变。只从精确当前提交快进推送，不覆盖分支/main/tag/Release，不执行任意补丁。源码和版本回执留存。若分支前移导致push冲突，直接失败，不force。

本地既有版本递增单测通过；workflow/YAML和写入路径/事件/权限复核通过。bot版本提交不会自动触发全部下游CI，后续必须由新的正常审查提交触发新版本完整双平台验证。版本准备本身不等同构建、安装验收或发布。暂未纳入正在本地编写的交付汇总代码。

自审93/100，只准入有界版本准备；Windows原生/已安装及真实ChatGPT仍有门禁缺口，禁止合并发布。
