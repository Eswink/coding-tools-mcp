# 聊天授权迭代 v23：标准账户候选恢复、边界回归和真实证据门禁

## 精确恢复
基线 `1584a1629cecf4c3fe1a7006347f98ead87ccbf8`，树 `e6463ff6a61e60943889c85afbbaa20766562c1c`；下载 Actions 34476646404 工件 10151753419，外层 SHA-256 `6b684cb321cc81fba6ab90447d3cfead65e615e3ec885e5575971f96fc4aed72`，重建相同内容树。既有树 caef8f1 没有分支提交，不能称已交付。标准账户脚本、令牌适配器、七项契约逐字还原，分别核对已有 blob 01964e0、8a6afec、ce970e3。保留全部产品代码、依赖与 0.3.0 版本不变。

## 修复范围
- 仅在本仓库一次性 Windows 托管 Runner 创建随机标准账户，系统正常登录加载新配置文件；真实令牌须 non-elevated、medium、无受限标记；不手工裁剪该用户令牌。
- 凭据只在父进程内存中，不进入参数、环境或证据。复制的测试脚本校验摘要；应用与安装载荷保持原字节。仅给新 SID 授予夹具目录和两个确切二进制所需权限。
- 进程先挂起、绑定私有 Job，再启动。清理仅操作本次账户/SID/标记和私有目录；任何清理或凭据扫描失败使结果失败。
- 接入前轮未提交的 v19 补充：0/8 不再预填原生/OAuth/IPC 成功；已知宿主退出立即失败；八阶段精确顺序、实际源码/运行/版本/类型/摘要及清理均由严格证据门禁检查。新增文件明确区分“本地补充”历史，未覆盖远端已有评审。
- Windows debug 与实际 NSIS 安装路径均切换相同标准账户入口；Ubuntu 四安装组合门禁保持。新八项路径测试覆盖仓库和临时输出、根目录/邻居/遍历/外国二进制拒绝、源码身份不匹配以及原字节保留。

## 实际自审
64 项辅助回归、Python 编译、diff 空白检查通过。CI 在 Ubuntu source 和 Windows 原生预检运行全部辅助回归，不删除或忽略测试。GitNexus 上游影响 LOW；原生 run 一个直接入口，动态导入图谱无法完全识别，人工复核实际调用点。未触及产品函数、沙箱配置、操作系统全局安全策略或已有账户。

自审 **94/100**：修复范围29/30，安全边界25/25，实测证据20/25，可追溯20/20。只允许提交候选进入 CI；Windows 标准账户真实运行/清理尚未完成，不能由契约测试替代。整体发布门槛仍要求评分≥95且全部硬门禁通过。测试会话元数据仍为合成数据；真实 ChatGPT 双窗口与可信来源验收未完成。

## 后续门禁
读取本提交真实 Windows 原生和 NSIS 结果；失败时保留首个错误、按实际证据修复。五安装组合、双端 Rust/前端全量都需来自同一最终提交；来源与发布附件校验、真实公网客户端授权边界仍独立验收，不以本分数自动合并或发布。

依据：Microsoft CreateProcessWithTokenW 官方文档规定 LOGON_WITH_PROFILE 和 null desktop 的目标用户权限行为；OpenAI Apps/Plugins 认证文档要求服务器自行验证认证授权，会话关联元数据不等于身份凭据。
