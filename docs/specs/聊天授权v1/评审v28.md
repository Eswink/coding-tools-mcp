# Windows 原生 v28：保留真正的引导导入错误

v27实际运行34509189849，工件10165302317 SHA256 72f6b767beaef3d3c460bee0ea757f5b49b23c84c698f7d0262935f2242ec875：pythonw引导日志首次出现bootstrap_entered，挂起计数1，实际宿主退出1，类型ImportError。未达到业务入口。标准账户、配置、进程已清理且凭据扫描无命中。v27不再是480秒无证据挂起；当前仍不能断言是哪一个DLL。v27本地候选92，实际完整交付80分打回。

v28最小变更仅引导日志：原先再次抛出异常时文件上下文已经关闭，导致Python最终异常正文丢失。现在在文件关闭前写有限20帧完整异常（不捕获locals），finally取消故障记录并恢复原stdio/argv。SystemExit不写重复业务堆栈，本机OAuth之后的异常由业务层自行处理。新增2个行为测试直接注入ImportError/SystemExit，核验真实文件内容及资源恢复，不只检查字符串。产品与真实8阶段不变，影响LOW。

既有68+4、新增2共74项本地辅助通过，变更检查4文件。执行者自审94/100，只准入CI，未声称Windows业务通过或解决具体DLL根因。仅真实ChatGPT账号与公网客户端联调保留本地。
