# 任务清单：聊天授权v1

## 概述
实现第三方案；审批只在可信本机；网络业务默认拒绝。当前范围仅授权及其必要OAuth/运行域接入，不重构其他产品能力。

## 交付物清单（Scope-lock）
预计新建14至20个文件、修改12至18个文件、约30个函数；实现完成后以真实git diff更新清单。新增模块包括授权模型、状态存储、统一闸门、会话运行域、OAuth身份验证、IPC、Svelte API/面板、负向测试及本三份规格。适配原有500行以上文件时只加薄入口，不扩大职责。

## 任务列表
### 阶段 1: 准备工作
- [x] 1.1 核对当前认证和统一入口，生成影响证据并校验规格。
  - **证据块**: `src-tauri/src/tools/dispatch.rs:55`，`pub fn call_tool(ctx: &ToolContext, name: &str, args: &Value) -> Value` 是唯一执行入口；`auth/oauth_flow.rs:76`返回bool而非principal。
  - **涉及文件**: 本需求、设计、任务文档各少于200行。
  - _需求: FR-1, FR-6_ ｜ _设计: 架构设计、风险评估_
### 阶段 2: 核心实现
- [ ] 2.1 实现有界Grant状态机、只读/开发scope、90秒pending及本机批准撤销。
  - **证据块**: `src-tauri/src/auth/mod.rs:1` 现有只有PublicOrigin、bearer、oauth_flow导出；无聊天授权模型。
  - **涉及文件**: auth新增模型/存储/闸门各少于400行。
  - _需求: FR-1, FR-2, FR-4_ ｜ _设计: 数据模型、API设计_
- [ ] 2.2 传递OAuth主体并在统一入口绑定MCP会话，拒绝无身份Actions。
  - **证据块**: `mcp/listener.rs:180` mcp_post先require_mcp_auth再handle_request；`mcp/server.rs:78`直接call_tool共享state；`actions/listener.rs:339` execute_action调用同一内核。
  - **涉及文件**: auth/oauth_flow拆分身份模块、mcp/listener、mcp/server、actions/listener、dispatch，每个新模块少于400行。
  - _需求: FR-1, FR-4, FR-5_ ｜ _设计: 架构设计、决策1、决策4_
- [ ] 2.3 隔离cwd/进程/异步任务/harness/history，并保留本机取消能力。
  - **证据块**: `tools/context.rs:10` ToolContext持有共享sessions/exec_tasks/harness；`tools/异步命令v1.rs:124`后台经call_tool重新执行。
  - **涉及文件**: tools新增运行域少于350行、context、commands/异步任务v2、history薄接入。
  - _需求: FR-3_ ｜ _设计: 决策2、决策3_
- [ ] 2.4 接入桌面审批面板、撤销全部和独占状态，修正启动提示词授权顺序。
  - **证据块**: `src/routes/workspace/[id]/+page.svelte:16` 已有ChatGptSessionPrompt；`src-tauri/src/lib.rs`集中注册Tauri IPC。
  - **涉及文件**: 新增API/组件/IPC各少于300行、既有页面仅增加挂载。
  - _需求: FR-2, FR-5_ ｜ _设计: API设计_
### 阶段 3: 集成测试
- [ ] 3.1 对照验收标准核验未授权、跨会话、scope、撤销、过期、后台执行及OAuth失败路径。
  - **证据块**: `src-tauri/tests/common/mod.rs:148` 测试通过真实tools::call_tool，不用UI模拟代替安全边界；已有网络异步回归可扩展。
  - **涉及文件**: 新增授权/隔离/OAuth测试各少于500行；CI验证工作流及发布边界记录。
  - _需求: FR-1, FR-2, FR-3, FR-4, FR-5, FR-6_ ｜ _设计: 测试策略_

## 检查点
- [x] 阶段1：规则和当前代码已读，HIGH风险已报告。
- [ ] 阶段2：统一闸门与UI连接完成，代码审查无越权发现。
- [ ] 阶段3：提供真实测试退出码；公网/双窗口未测保持未验收。

## 需求覆盖矩阵
| 需求ID | 设计章节 | 任务编号 | 状态 |
|---|---|---|---|
| FR-1 | 架构设计 | 1.1,2.1,2.2,3.1 | 进行中 |
| FR-2 | 数据模型 | 2.1,2.4,3.1 | 未完成 |
| FR-3 | 决策3 | 2.3,3.1 | 未完成 |
| FR-4 | API设计 | 2.1,2.2,3.1 | 未完成 |
| FR-5 | 决策1、决策4 | 2.2,2.4,3.1 | 未完成 |
| FR-6 | 测试策略 | 1.1,3.1 | 进行中 |

## 文件变更清单
以提交差异及最终验收记录的逐文件manifest为准；上述涉及文件为范围约束，范围变化必须回写本任务记录。

## 检查清单
- [x] 每条任务含真实证据、需求回链和新增文件行数预算。
- [ ] 实际交付数量核对、测试和独立环境验收完成。
