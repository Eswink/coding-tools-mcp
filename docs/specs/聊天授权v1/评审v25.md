# 迭代 v25：修正标准账户外层启动参数

基线：0185dfeb33a64af324b5946c3b91e570e104abf7；原始内容树547a894d9e00aa2814d4597a6e60bff7df3fd52f，已从Actions工件10159139099下载并重算摘要/内容树。

现象：Windows标准账户已取得令牌，但CreateProcessWithTokenW同步返回WinError 87；失败发生在真正应用执行前，不能归因OAuth或审批逻辑。微软函数文档说明CREATE_NEW_CONSOLE默认启用，Process Creation Flags文档说明该标志不能与DETACHED_PROCESS组合；现有调用显式传0x4|0x400|0x8，存在确定的API契约冲突。其他路径、权限、ABI竞争假设保留，真实运行仍需验证。

最小修复：仅从外层CreateProcessWithTokenW移除DETACHED_PROCESS；保留CREATE_SUSPENDED、CREATE_UNICODE_ENVIRONMENT、LOGON_WITH_PROFILE、NULL环境（由新用户配置生成）、标准账户校验、先纳入私有Job后恢复、清理及凭据扫描。内层CreateProcessW与真正应用Popen的DETACHED_PROCESS保持不变。不修改产品代码、授权策略、沙箱、版本或依赖。

验证：新四项契约在基线3/4（位掩码1036!=1028），修复后4/4；授权辅助回归合计68/68，无跳过。GitNexus已索引并执行run的精确文件影响分析，LOW、2直接匹配，动态工作流入口人工复核；全文检索扩展因离线未加载，未伪称全面语义检索成功。提交前diff/check与变更检测。

自审94/100：最小修复30/30，安全25/25，回归19/25，追溯20/20；仅准入真实CI。v24整体验收85/100打回；本轮不得将本地68项视为Windows原生或发布通过。

用户本轮明确授权：若Windows原生CI问题经修复仍无法解决，可交付标注“待本地核验”的预发布包。该例外不豁免Rust/HTTP授权隔离、安全测试、Ubuntu安装矩阵、实际构建/摘要/版本检查；不得把失败改记PASS，不覆盖旧Release。

参考：
- https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-createprocesswithtokenw
- https://learn.microsoft.com/en-us/windows/win32/procthread/process-creation-flags
