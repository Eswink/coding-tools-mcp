# 本地补充评审 v19：对齐并发远端并校正最终证据

## 基线与去重
本轮末再次读取远端，功能分支由 `9d23813` 推进至 `6e9e0379b903053bb871ecdade108cac7ea2f89d`。下载 Actions `34449769487` 的源码工件 `10141116616`，外层 SHA-256 `94510cf3d152a39cb7e861875026d52103e83c637198b95694995d1349268abe`，重建内容树 `7d03a072a8c2ae67882f94c88b133ed1f3e16ab0`。这是重新核验的基线，不把同时出现的远端提交冒领为本地提交成果。

远端已经保留 DETACHED_PROCESS 和 Bash/PowerShell 八组对照；补充补丁不再修改 Windows非提升进程v12.py、不覆盖远端评审v16/v17，也不删除 Windows启动对照v16 工作流。此前本地 v16–v18 记录另存为本地补充评审。修复一次对齐中暴露的常量引用不匹配：探针及测试统一引用远端 NATIVE_CREATION_FLAGS，不新增第二个常量。

## 新的实际失败
远端 v17 Windows 原生工件 `10141337444` 外层 SHA-256 `31c60ec496cbce1695e18e3144cf8e2cef333250decc65af17f6c58641ee769c`。Python 宿主现在能够启动并写出子进程记录：应用子进程 exit_code=3221225794、0xc0000142；随后外层宿主退出0xffffffff。两者不可混为一谈。原生授权仍为0/8。启动对照成功不等于真正应用通过，不能继续把 v17 控制台修复当成已经解决完整 Windows 故障。

## 本地验证与复审
对齐前先跑回归，常量不匹配触发5个错误，初审85/100并打回；修复后七个脚本总计45项Python单元/故障注入/契约测试通过（包含远端新增1项）。三个工作流保持 contents: read，13段Bash通过语法校验，保留原生审批与安装载荷检查。图谱对探针run上游为1个直接入口、LOW，动态CI调用人工复核。补充差异不修改 Rust产品、前端、依赖、版本或授权策略。

本地对齐子目标复审96/100（功能29/30、回归29/30、兼容19/20、追溯19/20）；整体仍BLOCKED。代码审查工具只收集真实差异和给出指南，不是独立安全审批。计划原有v16 runtimeEvidence曾误记v17本地SHA，最终检查点必须明确更正，且每轮本地SHA与GitHub远端SHA分开保存。

## 剩余硬门禁
本次工具通道只有GitHub读取/下载，没有提交/调度/合并/Release写入。尚未把本地补充补丁提交远端，也未运行新的Windows完整验收；目前没有本轮本地Rust/Svelte编译环境。后续应先核验真正应用在中等权限、私有Job、同桌面中的进程创建差异，不能仅延长超时或放宽权限。修复后必须五组合安装后八阶段验收、双平台完整基线、真实ChatGPT双窗口/可信请求来源、最终合并和公开附件再下载校验全部通过才发布。没有执行/通过的阶段不得靠评分代替。
