# Windows 原生恢复 v31：普通凭据登录与真实子身份

## 失败证据与范围
v29运行34511903467工件10166338985（SHA256 04a0f5ca3b11659f7eac4d22881518513d3679a82f5b85fde912f7cf54c8be51）重新下载核验：标准账户宿主进入引导，_ctypes DLL初始化失败；有限桌面AccessCheck前false后true且清理完成，仍未进入应用。v29不是成功修复，实际交付评分82/100打回。v30仅新增只读源码取证，子目标95/100，不计Windows成功。

## 控制变量与最小候选
将外层CreateProcessWithTokenW换为微软推荐的普通凭据CreateProcessWithLogonW。沿用同安装pythonw、真实用户profile和空环境、挂起创建、先归属私有Job再恢复线程；不改变内层已验证的进程启动。停止在真实入口调用手工桌面DACL修改，旧ACL回归保留。此API的成功返回仍早于DLL初始化，NULL桌面不是现代Windows下GUI可用的保证，不能先宣称根因已解决。

以前只核验预检LogonUser令牌，本轮补充实际已创建宿主的TokenUser、TokenElevation、TokenIntegrityLevel、TokenHasRestrictions、TokenSessionId；要求精确临时账户SID、非提升medium无受限令牌、与调用方相同且非零的交互会话。从实际子令牌读取profile后再恢复线程。错账户、错会话和未知状态拒绝，查询句柄全部关闭；清理失败仍失败。账号随机口令只传原生登录API，不写argv/env/清单/日志，现有UTF8与UTF16凭据扫描、账号/配置/进程回收保留。

这是测试环境身份路径的HIGH风险变更，已提前报告；产品、OAuth策略、版本、依赖和沙箱不变。图谱run上游LOW；main为实际调用者，grant到run为subprocess.run名称碰撞，动态工作流入口已人工复核。AccountApi绑定及被改测试符号均已查影响。

## 回归与自审
新增测试先失败（实现不存在），首轮适配测试引号语法错误打回（88/100）；修正后追加句柄关闭失败注入，完整91项辅助回归通过，零跳过。原有API契约随替换调整参数位置，仍检查profile、空环境、挂起、Job先于Resume及无沙箱绕过；没有删除业务断言。新增真实子身份/会话拒绝、大小边界、失败句柄回收与真实入口顺序测试。

候选自审95/100（实现28/30，安全25/25，回归22/25，追溯20/20），仅准入实际CI；Windows八阶段和双平台安装矩阵未通过前不合并、不发布，不使用v0.3.0的延期例外。只有真实ChatGPT账号/双窗口元数据/公网联调保留用户本地。

## 官方依据
- https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-createprocesswithtokenw
- https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-createprocesswithlogonw
- https://learn.microsoft.com/en-us/windows/win32/api/securitybaseapi/nf-securitybaseapi-gettokeninformation
