# 聊天授权迭代 v9：Windows 原生附加修复

## v8 结果及打回
c621482 对应 Actions 34438583134 失败。工件 10137233457 外层 SHA-256 为 4811d58ff09d1bf7b52e0c73a377b66f45af789351ffc9b9bea2cf7d53361000，已逐字复核。Windows debug 程序构建成功，但 EdgeDriver launch 模式等待 DevToolsActivePort 失败，八组原生断言一组也未运行。v8 运行后自审 72/100，打回，不计原生通过。

## 本轮修复
根据 Microsoft WebView2 WebDriver 官方 attach 模式，测试器通过 Popen 启动精确应用，仅对该子进程设置回环调试参数，待实际 WebView2 JSON 端点就绪后使用 debuggerAddress 附加。不修改注册表、产品代码、OAuth、Grant、沙箱策略，不启用 acceptInsecureCerts，不重放审批/IPC。应用和驱动均持有本调用创建的进程句柄，失败及结束时分别清理，避免应用因脱离驱动进程树而残留。

## 影响与证据
GitNexus 1.6.9 对 WindowsNativeSession、__init__、close、windows_capabilities 及变更的测试方法执行上游分析：LOW，直接调用最多1个；动态工厂/CI由人工复核。索引5438节点、13311边；FTS离线缺失，不代替全文安全审计。执行变更检测并以真实 Git diff 复核两脚本范围；设置本地 core.quotepath=false 以便工具处理中文路径，不改远端配置。

本地五项适配器单测、四项共同harness单测及Python编译通过。新增端口类型/范围拒绝、子进程环境隔离、两进程清理及异常清理保证。此证据不是 Windows 原生实测。

## 评分与门禁
候选自审93/100：修复与针对性回归可进入CI，但未达到最终95分及零阻断标准。下一步从本次精确提交执行两端Rust/前端、两Ubuntu原生和Windows原生，再验收DEB/AppImage/NSIS安装载荷。真实ChatGPT双窗口与可信公网入口仍未验证，不能将合成元数据标为真实宿主证明。本轮不合并、不发布。

依据：https://learn.microsoft.com/en-us/microsoft-edge/webview2/how-to/webdriver#step-4b-attaching-microsoft-edge-webdriver-to-a-running-webview2-app
