# v0.5.0 stable release and repository README closure

Status: IMPLEMENTING. User authorization: “接下来继续任务，直到双端完成发布，并且为release不用prerelease，然后把文档的readme.md完成修改，修改为符合我们仓库版本的！”

## Scope and immutable baseline

Promote existing Release 388174565 / v0.5.0 from pre-release to full Release and Latest; update README.md and README.en.md for Eswink/coding-tools-mcp. The product source remains 63261e0c9ae4b0c6befdbe5ae8caff7f5d32c076, tree 01b11a1088d82501a1e579801ce6ac774ab5d43a. Do not rebuild, rename, replace or delete any of the seven existing assets; do not move v0.5.0 or release/ui-v0.5.0. Main may advance only for the allowlisted documentation and promotion tooling. No UI/Rust/API/dependency change, new product version, PR #11 integration, Nginx change or credential change.

The original build34810770540 keeps its failure conclusion: sixteen source/build/native prerequisite jobs passed, while the post-publication tag lookup failed. Independent read-only run34812656272 subsequently verified the original evidence and anonymous bytes. Neither conclusion is rewritten by promotion.

## Execution rounds

1. Read current instructions, source, README, release metadata and open PRs; restore pinned Probe4.0.1 or record failure. Reconstruct the original source tree and verify all attached asset bytes.
2. Rewrite both READMEs: this fork's downloads/platforms/version, five-page UI, OAuth plus local approval, single owner/draining, configurable token/lease lifetime, conversation history boundaries, existing tool/developer commands, migration and known limits. Preserve upstream attribution. Remove obsolete upstream download badges and unsupported macOS release claims.
3. Add a current stable release note without replacing historical verification attachments. Clearly distinguish product-source SHA from documentation/promotion SHA. Document that 0.5.0's built-in update checker still targets upstream: use this fork's Release links; this metadata-only operation cannot change compiled update URLs.
4. Add a version-specific promotion gate with fail-closed tests. Re-run the ORIGINAL unmodified source/UI/native evidence composer using the original source/export and original delivered bytes. Verify original run and job identities. Anonymously download every public asset before mutation. Only allow a single PATCH to this one release's name/body/prerelease/make_latest; no upload, tag or delete API. Require main==promotion SHA before mutation; verify tags, source and seven asset IDs/digests remain unchanged afterward. Persist a receipt even on a post-mutation failure; never retry a write blindly.
5. Validate Markdown paths/downloads/version/defaults and change allowlist; run promotion negative contracts and pre-existing publisher/native contracts. Push one bounded PR, inspect its exact diff and CI, merge only when green. No duplicate implementation PR.
6. Start a release/stable-v0.5.0 workflow from the merged main. Repeat original evidence gates, promote metadata, check prerelease=false/draft=false and /releases/latest, then anonymously check bytes again. Save a machine-readable receipt and PR closure comment; deliver the unchanged Windows/Ubuntu packages and current guides.

## Hard limits and policy

Stable is the user-authorized release channel, not a new claim about untested environments. Keep real ChatGPT long-duration refresh, OS notification-banner visibility, Windows signing, FUSE/Wayland/hardware coverage and logical-versus-OS isolation limitations. Historical bundled Verification/Release-scope keep their original prerelease labels as immutable build-time evidence; the live Release and stable note supersede only the channel designation.

MCP Probe discovery found no matching installed connector. Known launcher resume failed127; pinned self-install timed out; exact-version offline resume and impact returned ENOTCACHED. The full raw Git archive reconstructs tree01b11a1; ordinary git add originally omitted ignored-but-tracked entries and produced the wrong tree, so verification uses per-entry raw hashes/modes and a complete index. No graph/managed-plan PASS is claimed. New promotion code has no product callers; its sole high-impact side effect is bounded Release metadata editing. Manual call-chain/allowlist review and real runtime tests are the explicit fallback.

## Completion evidence

To be recorded in the merged PR and generated stable-promotion-receipt.json: documentation commit, product SHA, original build identity and preserved failure, original gates summary, promotion run, stable/latest state, exactly seven unchanged asset identities, anonymous downloads before/after, allowed PATCH count. Do not mark this file's implementing status as proof of completion; the final receipt is authoritative.
